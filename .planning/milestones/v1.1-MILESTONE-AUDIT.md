---
milestone: v1.1
milestone_name: Typed Models & Browser Reach
audited: 2026-06-02
status: tech_debt
scores:
  requirements: 13/13   # all satisfied; 1 (BROWSER-03) single-source corroboration (doc gap, not delivery gap)
  phases: 4/4           # all VERIFICATION.md passed
  integration: 4/4      # all cross-phase connections wired
  flows: 5/8            # 5 fully covered, 2 partial, 1 missing (all tech-debt)
test_suite:
  command: "uv run pytest packages/ -m \"not integration\" -q"
  passed: 352
  failed: 0
  deselected: 3
gaps:
  requirements: []      # no unsatisfied requirements — FAIL gate not triggered
  integration: []       # no broken cross-phase wiring
  flows:
    - id: "codegen-to-read-roundtrip"
      status: "missing"
      severity: "tech-debt"
      reqs: ["TYPED-01", "TYPED-02", "TYPED-03"]
      evidence: "No test feeds a codegen-generated model class through client.read() dispatch. Both halves tested in isolation; the codegen-output ↔ read-dispatch contract is verified by inspection only."
    - id: "wire-transforms-through-dispatch"
      status: "partial"
      severity: "tech-debt"
      reqs: ["TYPED-06"]
      evidence: "Ref / date / datetime transforms tested at OdooBaseModel.model_validate level (test_pydantic_transform.py) but no test_typed_dispatch.py case uses a model with Ref/date fields, so transforms are not exercised through the full client.read dispatch chain."
tech_debt:
  - phase: 08-pyodide-spike
    items:
      - "spikes/08-pyodide/run_spike.py:16 hardcodes password=admin in a committed file (ACA endpoint torn down; trivial default; no rotation needed — but it IS committed)."
      - "08-03-SUMMARY.md threat-surface claim 'credentials never committed' is factually incorrect — run_spike.py:16 commits password=admin. Correct the SUMMARY text."
      - "Strategy 1 (stock httpx in Pyodide) worked unexpectedly; routing mechanism unconfirmed — deferred to v2.0 investigation."
  - phase: 07-pydantic-cli-generator
    items:
      - "test_cli.py: test_generate_auth_error_exits_1 / test_generate_network_error_exits_1 leave _generate_async coroutines unawaited (RuntimeWarning noise in CI output). Tests pass; cosmetic."
  - phase: 06-transport-seam-typed-models-core
    items:
      - "BROWSER-03 has no requirements-completed frontmatter entry in any Phase 8 SUMMARY (08-04-SUMMARY missing the key); corroborated by VERIFICATION + traceability only. Add to 08-04-SUMMARY for full 3-source corroboration."
nyquist:
  enabled: false        # workflow.nyquist_validation: false in config.json — section skipped
  overall: "disabled"
deferred_to_v2:
  - "BROWSER-F1: godoo[browser] extra with spike-validated PyfetchTransport (GO verdict, ADR-0001)"
  - "BROWSER-F2: relax Python floor for Pyodide runtime (Option A — defer until Pyodide ships CPython >=3.14)"
  - "TYPED-F1: nested relational model fetch"
  - "TYPED-F2: typed write/create paths"
---

# Milestone v1.1 — Audit Report

**Milestone:** v1.1 Typed Models & Browser Reach (Phases 5–8)
**Audited:** 2026-06-02
**Status:** `tech_debt` — all requirements met, no critical blockers, accumulated tech-debt needs review
**Method:** read all phase VERIFICATION.md + SUMMARY frontmatter (3-source cross-reference), empirical e2e test-coverage validation (live `uv run pytest`), cross-phase integration check.

## Summary

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements satisfied | 13/13 | 12 full 3-source; BROWSER-03 single-source (doc gap, not delivery gap) |
| Phases verified | 4/4 | every phase has `VERIFICATION.md: passed` |
| Cross-phase integration | 4/4 | all connections wired, 0 orphaned exports |
| E2E flow coverage | 5/8 fully | 2 partial + 1 missing — all tech-debt, no broken flows |
| Unit test suite | 352 passed / 0 failed | `uv run pytest packages/ -m "not integration"` (3 integration tests deselected) |

**No blockers. No unsatisfied requirements. The FAIL gate is not triggered.**

## Requirements Coverage (3-source cross-reference)

VERIFICATION.md (passed + listed) × SUMMARY frontmatter × REQUIREMENTS.md traceability `[x]`.

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | Traceability | Verdict |
|--------|-------|--------------|---------------------|--------------|---------|
| PKG-01 | 5 | passed | ✓ 05-01 | `[x]` | **satisfied** |
| PKG-02 | 5 | passed | ✓ 05-01 | `[x]` | **satisfied** |
| PKG-03 | 5 | passed | ✓ 05-02 | `[x]` | **satisfied** |
| BROWSER-01 | 6 | passed | ✓ 06-01 | `[x]` | **satisfied** |
| TYPED-06 | 6 | passed | ✓ 06-02 | `[x]` | **satisfied** |
| TYPED-07 | 6 | passed | ✓ 06-02 | `[x]` | **satisfied** |
| TYPED-03 | 6 | passed | ✓ 06-03 | `[x]` | **satisfied** |
| TYPED-04 | 6 | passed | ✓ 06-03 | `[x]` | **satisfied** |
| TYPED-05 | 6 | passed | ✓ 06-03 | `[x]` | **satisfied** |
| TYPED-01 | 7 | passed | ✓ 07-01/07-02 | `[x]` | **satisfied** |
| TYPED-02 | 7 | passed | ✓ 07-01/07-02 | `[x]` | **satisfied** |
| BROWSER-02 | 8 | passed | ✓ 08-02 | `[x]` | **satisfied** |
| BROWSER-03 | 8 | passed | ✗ (no SUMMARY key) | `[x]` | **satisfied** (single-source corroboration — see tech-debt) |

**Orphaned requirements:** none. All 13 appear in at least one VERIFICATION.md with evidence.

## Cross-Phase Integration

All four cross-phase connections verified WIRED (evidence from source + passing suite):

1. **Phase 5 rename → Phase 6/7 imports** — zero stale `packages/godoo/` refs in operative source/config; `packages/godoo-client/src/godoo/__init__.py` absent (PEP 420 intact); all imports resolve (352 tests pass).
2. **Phase 6 typed layer → `client.read`/`search_read` dispatch** — `@overload` pairs + `if hasattr(model, "__odoo_model__")` duck-typed gate at `client.py:220`; lazy `_pydantic_transform` import inside the branch.
3. **Phase 7 generated models → Phase 6 dispatch contract** — generated files emit `from godoo.client._pydantic_transform import OdooBaseModel` + `__odoo_model__: ClassVar[str]`; structurally compatible with `model_validate` dispatch.
4. **Phase 6 transport seam (BROWSER-01) → Phase 8 `PyfetchTransport`** — `OdooClientConfig.transport_factory` is the injection point; `spikes/08-pyodide/transport_pyfetch.py` implements the exact 5-member `Transport` Protocol.

## E2E Test-Coverage Validation (empirical)

Live `uv run pytest packages/ -m "not integration" -q` → **352 passed, 0 failed, 3 deselected.**

| Flow | Status | Evidence |
|------|--------|----------|
| Typed-read happy path (`read(ModelClass, ids)` → `list[ModelClass]`, False→None) | COVERED | `test_typed_dispatch.py::test_read_typed_path_returns_models`, `::test_read_typed_applies_wire_transforms` |
| Raw path unchanged (`read("res.partner", ids)` → `list[dict]`) | COVERED | `test_typed_dispatch.py::test_read_str_path_returns_dicts` |
| Pydantic isolation (default import has no pydantic) | COVERED | `test_typed_isolation.py::test_pydantic_not_imported_by_default` (subprocess asserts `'pydantic' not in sys.modules`) |
| Transport seam injection | COVERED | `test_transport_protocol.py::test_jsonrpc_transport_satisfies_protocol` |
| PEP 420 guard (`godoo.__file__ is None`) | COVERED | `test_namespace.py::test_godoo_is_namespace_package` |
| Wire transforms (m2o→Ref, date/datetime) through dispatch | **PARTIAL** | transforms tested at `model_validate` level (`test_pydantic_transform.py`) but not via `client.read` dispatch — TYPED-06 |
| CLI generate flow (compilable Pydantic files) | **PARTIAL** | `test_cli.py` checks file content strings (mocked RPC); compile/import asserted separately at codegen level (`test_codegen.py::test_generate_valid_python_compiles`, `::test_generate_importlib_roundtrip`) — TYPED-01/02 |
| Codegen → read round-trip (generate → load → `client.read(LoadedClass, ids)`) | **MISSING** | no test joins the two halves — TYPED-01/02 → TYPED-03 boundary verified by inspection only |

**Integration tests:** `-m integration` (Docker/testcontainers) tests exist but were not run in this audit (require Docker). They cover live-instance client behaviour, not the typed-dispatch round-trip gap above.

**Pyodide (BROWSER-02/03):** correctly treated as non-shipping decision artifacts. `08-SPIKE.md` (in-browser HTTPS JSON-RPC run, uid+users returned via PyfetchTransport) and `docs/adr/0001-*` (GO verdict, Strategy 3, Python-floor Option A) both exist. No unit test expected.

## Tech-Debt Register

**Phase 8 — Pyodide spike:**
- `spikes/08-pyodide/run_spike.py:16` hardcodes `password=admin` in a **committed** file. The ACA endpoint is torn down and `admin` is a trivially guessable default — no rotation needed — but it is committed.
- `08-03-SUMMARY.md` threat-surface claim *"credentials never committed"* is **factually incorrect** given the above. Correct the SUMMARY text.
- Strategy 1 (stock httpx in Pyodide) worked unexpectedly; routing mechanism unconfirmed — deferred to v2.0.

**Phase 7 — CLI:**
- `test_cli.py` error-path tests leave `_generate_async` coroutines unawaited → `RuntimeWarning` noise in CI output. Tests pass; cosmetic.

**Coverage gaps (carry as test-debt for v2 or a quick task):**
- Add `test_generate_importlib_roundtrip_with_client_read` to close the codegen→read round-trip gap (single highest-value test).
- Add a dispatch test using a model with `Ref`/`date` fields to exercise wire transforms through `client.read`.

**Documentation:**
- Add `requirements-completed: [BROWSER-03]` to `08-04-SUMMARY.md` frontmatter for full 3-source corroboration.

## Nyquist Compliance

`workflow.nyquist_validation: false` in `.planning/config.json` — Nyquist validation disabled project-wide. Section skipped.

## Verdict

The v1.1 milestone achieved its definition of done: all 13 requirements satisfied, every phase independently verified, cross-phase integration wired, and the unit suite green (352/0). The Pyodide spike delivered its decision artifact (ADR-0001, GO → escalated to v2.0). Outstanding items are tech-debt — two test-coverage gaps at the codegen↔typed-read boundary and a doc correction in the Phase 8 spike summary — none of which blocks shipping the milestone. **Recommend: complete the milestone, tracking the tech-debt items in the backlog.**
