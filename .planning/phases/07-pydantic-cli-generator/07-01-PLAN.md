---
phase: 07-pydantic-cli-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/godoo-introspection/src/godoo/introspection/markers.py
  - packages/godoo-introspection/src/godoo/introspection/__init__.py
  - packages/godoo-introspection/src/godoo/introspection/type_mapper.py
  - packages/godoo-introspection/src/godoo/introspection/codegen.py
  - packages/godoo-introspection/tests/test_introspector.py
  - packages/godoo-introspection/tests/test_type_mapper.py
  - packages/godoo-introspection/tests/test_codegen.py
autonomous: true
requirements:
  - TYPED-01
  - TYPED-02

must_haves:
  truths:
    - "Generated Pydantic file for a model compiles without SyntaxError"
    - "Generated file imports from godoo.client._pydantic_transform (OdooBaseModel) and godoo.client.typed (Ref)"
    - "Each generated class carries __odoo_model__: ClassVar[str] set to the Odoo technical model name"
    - "id field emits as plain int (required, no Optional, no default)"
    - "Scalar fields emit as Optional[T] = None; bool emits as plain bool = False"
    - "selection fields emit as Optional[Literal['val1','val2',...]] = None (static) or Optional[str] = None (dynamic)"
    - "many2one target in-set emits as Optional[Ref[TargetClass]] = None with cross-import; degraded emits as Optional[Ref[int]] = None with trailing # model.name comment"
    - "x2many fields emit as list[int] = []"
    - "markers.py is deleted; FieldMeta is not exported from godoo.introspection"
    - "All unit tests (not integration) pass: uv run pytest packages/ -m 'not integration'"
  artifacts:
    - path: "packages/godoo-introspection/src/godoo/introspection/type_mapper.py"
      provides: "Pydantic field string mapper (pydantic_field_str function)"
      contains: "pydantic_field_str"
    - path: "packages/godoo-introspection/src/godoo/introspection/codegen.py"
      provides: "Pydantic model emitter (CodeGenerator with in_set constructor param)"
      contains: "OdooBaseModel"
    - path: "packages/godoo-introspection/tests/test_type_mapper.py"
      provides: "Tests for Pydantic type forms"
      contains: "Optional[str]"
    - path: "packages/godoo-introspection/tests/test_codegen.py"
      provides: "Tests for Pydantic emitter output"
      contains: "OdooBaseModel"
  key_links:
    - from: "packages/godoo-introspection/src/godoo/introspection/codegen.py"
      to: "packages/godoo-introspection/src/godoo/introspection/type_mapper.py"
      via: "from godoo.introspection.type_mapper import pydantic_field_str"
      pattern: "pydantic_field_str"
    - from: "packages/godoo-introspection/src/godoo/introspection/codegen.py"
      to: "packages/godoo-client/src/godoo/client/_pydantic_transform.py"
      via: "generated file contains 'from godoo.client._pydantic_transform import OdooBaseModel'"
      pattern: "OdooBaseModel"
---

<objective>
Replace the TypedDict emitter infrastructure with the Pydantic emitter: delete markers.py and the FieldMeta export, migrate type_mapper.py to Pydantic type forms, replace CodeGenerator.generate() with a Pydantic emitter body, and rewrite the TypedDict-specific tests with Pydantic assertions. After this plan the package imports cleanly, codegen and type_mapper tests pass, and the quality gate (ruff + mypy + pytest) is green.

Purpose: This is the core replacement — the TypedDict era ends here. Both codegen.py and type_mapper.py must change in the same plan to keep the package importable (codegen.py imports from type_mapper.py; splitting would leave a broken import between plans).
Output: Updated codegen.py (Pydantic emitter), updated type_mapper.py (pydantic_field_str function), updated __init__.py (no FieldMeta), deleted markers.py, rewritten test_type_mapper.py and test_codegen.py (Pydantic assertions), cleaned test_introspector.py (2 FieldMeta tests deleted).
</objective>

<execution_context>
@C:\Users\marc\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\marc\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pydantic-cli-generator/07-CONTEXT.md
@.planning/phases/07-pydantic-cli-generator/07-RESEARCH.md
@.planning/phases/07-pydantic-cli-generator/07-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/godoo-client/src/godoo/client/_pydantic_transform.py:
- OdooBaseModel(BaseModel) — base class generated files subclass; owns @model_validator wire transforms
- Generated files import: from godoo.client._pydantic_transform import OdooBaseModel

From packages/godoo-client/src/godoo/client/typed.py:
- Ref[T] — dataclass for many2one references: Ref(id=..., name=...)
- Generated files with any many2one fields import: from godoo.client.typed import Ref

From packages/godoo-introspection/src/godoo/introspection/types.py:
- FieldSchema(name, ttype, field_description, relation, relation_field, required, readonly,
              store, index, copy, translate, help, compute, depends, modules, on_delete,
              size, digits, selection, original_ttype, dynamic_selection) — dataclass
- ModelSchema(name, fields: dict[str, FieldSchema]) — dataclass

From packages/godoo-introspection/src/godoo/introspection/codegen.py (current, being replaced):
- _model_to_classname(model: str) -> str  — "res.partner" → "ResPartner" (KEEP verbatim)
- _model_to_filename(model: str) -> str   — "res.partner" → "res_partner.py" (KEEP verbatim)
- CodeGenerator.write() loop structure (lines 185-220) — KEEP structure, update generate() call

From packages/godoo-introspection/src/godoo/introspection/type_mapper.py (current, being replaced):
- python_type_str(field: FieldSchema) -> str  — DELETE this function, replace with pydantic_field_str
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete TypedDict artifacts and clean barrel / test_introspector.py</name>
  <files>
    packages/godoo-introspection/src/godoo/introspection/markers.py
    packages/godoo-introspection/src/godoo/introspection/__init__.py
    packages/godoo-introspection/tests/test_introspector.py
  </files>

  <read_first>
    - packages/godoo-introspection/src/godoo/introspection/markers.py — read full file to confirm contents before deletion
    - packages/godoo-introspection/src/godoo/introspection/__init__.py — read to confirm current exports (lines 1-13)
    - packages/godoo-introspection/tests/test_introspector.py — read to identify the FieldMeta import (line 11) and the two FieldMeta isolation tests (lines 41-68) to delete
    - .planning/phases/07-pydantic-cli-generator/07-PATTERNS.md §"__init__.py (barrel, update)" and §"tests/test_introspector.py (partial update)"
  </read_first>

  <action>
    Delete markers.py entirely (it is the TypedDict-era FieldMeta marker; no Pydantic replacement — D-01).

    In __init__.py: remove the line "from godoo.introspection.markers import FieldMeta" and remove "FieldMeta" from __all__. Remaining exports: CodeGenerator, FieldSchema, IntrospectionCache, Introspector, ModelSchema.

    In test_introspector.py: delete the import "from godoo.introspection.markers import FieldMeta" (line 11) and delete the two FieldMeta isolation tests: test_field_meta_hashable (lines 41-46) and test_field_meta_default_attrs (lines 47-68). All other tests in this file are preserved unchanged. Add from __future__ import annotations at top if not present (it is already present — verify before writing).

    NOTE: After this task, codegen.py still imports from type_mapper.py (which still has python_type_str), and codegen.py still imports from markers (actually codegen.py does NOT import markers — only the __init__.py barrel and test_introspector.py do). Verify: codegen.py line 8 imports python_type_str from type_mapper; codegen.py lines 10-14 import from introspector and types under TYPE_CHECKING. codegen.py does not import markers. So deleting markers.py and cleaning __init__.py + test_introspector.py leaves codegen.py and type_mapper.py in their old state but importable for this task boundary only.
  </action>

  <verify>
    <automated>uv run python -c "from godoo.introspection import CodeGenerator, FieldSchema, Introspector, IntrospectionCache, ModelSchema; print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - markers.py file does not exist on disk
    - "from godoo.introspection import FieldMeta" raises ImportError
    - "from godoo.introspection import CodeGenerator" succeeds
    - __init__.py __all__ contains exactly: CodeGenerator, FieldSchema, IntrospectionCache, Introspector, ModelSchema (no FieldMeta)
    - test_introspector.py has no import of markers and no test_field_meta_hashable or test_field_meta_default_attrs functions
    - uv run pytest packages/godoo-introspection/tests/test_introspector.py -m "not integration" passes (remaining 8 tests green)
  </acceptance_criteria>

  <done>markers.py deleted; FieldMeta removed from barrel and test_introspector.py; package still imports cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace type_mapper.py with Pydantic type forms and rewrite test_type_mapper.py</name>
  <files>
    packages/godoo-introspection/src/godoo/introspection/type_mapper.py
    packages/godoo-introspection/tests/test_type_mapper.py
  </files>

  <read_first>
    - packages/godoo-introspection/src/godoo/introspection/type_mapper.py — current content (being replaced)
    - packages/godoo-introspection/tests/test_type_mapper.py — current content (being replaced)
    - .planning/phases/07-pydantic-cli-generator/07-PATTERNS.md §"type_mapper.py (type mapper, transform — in-place replacement)" — full migration table, new signature, preserved module header
    - .planning/phases/07-pydantic-cli-generator/07-RESEARCH.md §"Pattern 3: type_mapper.py migration" — full ttype migration table and new function signature
    - .planning/phases/07-pydantic-cli-generator/07-CONTEXT.md §CF-03, CF-04, CF-05 (bool=False, Literal, list[int])
  </read_first>

  <behavior>
    - pydantic_field_str(_field("char"), frozenset(), fn) == ("Optional[str]", "None")
    - pydantic_field_str(_field("text"), frozenset(), fn) == ("Optional[str]", "None")
    - pydantic_field_str(_field("integer"), frozenset(), fn) == ("Optional[int]", "None")
    - pydantic_field_str(_field("float"), frozenset(), fn) == ("Optional[float]", "None")
    - pydantic_field_str(_field("monetary"), frozenset(), fn) == ("Optional[float]", "None")
    - pydantic_field_str(_field("boolean"), frozenset(), fn) == ("bool", "False")
    - pydantic_field_str(_field("date"), frozenset(), fn) == ("Optional[date]", "None")
    - pydantic_field_str(_field("datetime"), frozenset(), fn) == ("Optional[datetime]", "None")
    - pydantic_field_str(_field("binary"), frozenset(), fn) == ("Optional[str]", "None")
    - pydantic_field_str(many2one with relation="res.partner", in_set=frozenset({"res.partner"}), fn) == ("Optional[Ref[ResPartner]]", "None")
    - pydantic_field_str(many2one with relation="res.company", in_set=frozenset(), fn) annotation == "Optional[Ref[int]]"; default starts with "None  # res.company"
    - pydantic_field_str(_field("one2many"), frozenset(), fn) == ("list[int]", "[]")
    - pydantic_field_str(_field("many2many"), frozenset(), fn) == ("list[int]", "[]")
    - pydantic_field_str(selection with values [("draft","Draft"),("done","Done")], frozenset(), fn) == ('Optional[Literal[\'draft\', \'done\']]', "None")
    - pydantic_field_str(selection with empty values, frozenset(), fn) == ("Optional[str]", "None")
    - pydantic_field_str(_field("json"), frozenset(), fn) == ("Optional[dict[str, Any]]", "None")
    - pydantic_field_str(_field("properties"), frozenset(), fn) == ("Optional[dict[str, Any]]", "None")
    - pydantic_field_str(_field("__custom__"), frozenset(), fn) returns annotation starting with "Optional[Any]"
    - unknown ttype logs a WARNING on godoo_introspection.codegen logger
  </behavior>

  <action>
    Replace type_mapper.py with a new implementation. Preserve: module docstring, from __future__ import annotations, logging import, TYPE_CHECKING guard importing FieldSchema, logger = logging.getLogger("godoo_introspection.codegen"), frozenset grouping idiom.

    New public function signature: pydantic_field_str(field: FieldSchema, in_set: frozenset[str], classname_fn: Callable[[str], str]) -> tuple[str, str]
    - Returns (annotation_str, default_expr_str) where the field line will be assembled as: f"    {field_name}: {annotation_str} = {default_expr_str}"
    - Callable must be imported: from collections.abc import Callable (or typing.Callable under TYPE_CHECKING — use collections.abc since this is a runtime parameter)

    The old python_type_str function is deleted entirely.

    Full ttype mapping (per PATTERNS.md migration table):
    - char/text/html/image/binary/serialized/reference → ("Optional[str]", "None")
    - integer → ("Optional[int]", "None")
    - float/monetary → ("Optional[float]", "None")
    - boolean → ("bool", "False")
    - date → ("Optional[date]", "None") — note: import date from datetime in generated code; mapper returns the annotation string only; callers track imports
    - datetime → ("Optional[datetime]", "None")
    - many2one, target in in_set (field.relation in in_set) → (f"Optional[Ref[{classname_fn(field.relation)}]]", "None")
    - many2one, target NOT in in_set → ("Optional[Ref[int]]", f"None  # {field.relation}") — trailing comment on the default_expr
    - one2many/many2many → ("list[int]", "[]")
    - selection with field.selection non-empty (static) → (f"Optional[Literal[{', '.join(repr(v) for v, _ in field.selection)}]]", "None") — use repr() for string values per PATTERNS.md shared patterns §repr()
    - selection with field.selection empty (dynamic) → ("Optional[str]", "None")
    - json/properties → ("Optional[dict[str, Any]]", "None")
    - unknown ttype → ("Optional[Any]", "None") + logger.warning with ttype and field.name

    Preserve the frozenset grouping idiom for ttype groups sharing the same output. Rename sets appropriately (e.g. _OPTIONAL_STR_TTYPES for char/text/html/image/binary/serialized/reference).

    Replace test_type_mapper.py entirely. Write tests (per PATTERNS.md test list and RESEARCH.md test pattern). Preserve _field() helper. Import pydantic_field_str (not python_type_str). Import _model_to_classname from codegen for classname_fn argument in tests. Delete all 22 old assertions. Write new per-ttype assertions using the behavior list above. Preserve test_unknown_ttype_logs_warning with caplog pattern.
  </action>

  <verify>
    <automated>uv run pytest packages/godoo-introspection/tests/test_type_mapper.py -v</automated>
  </verify>

  <acceptance_criteria>
    - All new test_type_mapper.py tests pass (no old "str | Literal[False]" assertions remain)
    - python_type_str is NOT present in type_mapper.py (grep -c "def python_type_str" returns 0)
    - pydantic_field_str IS present in type_mapper.py
    - pydantic_field_str returns a tuple[str, str] in all cases
    - boolean ttype returns exactly ("bool", "False") — not Optional[bool]
    - many2one in_set returns annotation starting with "Optional[Ref[" and contains the classname
    - many2one not-in-set default_expr starts with "None  # " followed by the relation model name
    - selection static returns annotation containing "Literal[" with repr'd values
    - unknown ttype logs WARNING on godoo_introspection.codegen logger (caplog test passes)
    - uv run ruff check packages/godoo-introspection/src/godoo/introspection/type_mapper.py passes
    - uv run mypy packages/godoo-introspection/src passes (strict mode)
  </acceptance_criteria>

  <done>type_mapper.py replaced with pydantic_field_str function; test_type_mapper.py rewritten; all type_mapper tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace CodeGenerator with Pydantic emitter body and rewrite test_codegen.py</name>
  <files>
    packages/godoo-introspection/src/godoo/introspection/codegen.py
    packages/godoo-introspection/tests/test_codegen.py
  </files>

  <read_first>
    - packages/godoo-introspection/src/godoo/introspection/codegen.py — current full file (being replaced)
    - packages/godoo-introspection/tests/test_codegen.py — current full file (being replaced)
    - .planning/phases/07-pydantic-cli-generator/07-PATTERNS.md §"codegen.py (code emitter, transform — in-place replacement)" — preserve/delete lists, new generate() signature, cross-import pattern for circular relations, new generate() output template, write() loop structure
    - .planning/phases/07-pydantic-cli-generator/07-RESEARCH.md §"Pattern 1: Pydantic model file template" and §"Pattern 4: cross-import and circular-import handling" and test_codegen.py pattern
    - .planning/phases/07-pydantic-cli-generator/07-CONTEXT.md §CF-01 through CF-06, D-04
    - packages/godoo-client/src/godoo/client/_pydantic_transform.py — verify OdooBaseModel import path
    - packages/godoo-client/src/godoo/client/typed.py — verify Ref import path

    NOTE (signature authority): RESEARCH.md code examples in §"Code Examples" show `gen.generate(schema, in_set=frozenset())` as a per-call argument. This form is superseded. The authoritative signature is `CodeGenerator(introspector, in_set=frozenset(...))` at construction with `generate(self, schema: ModelSchema) -> str` taking no `in_set` argument. Follow the action below, not those examples.
  </read_first>

  <behavior>
    - gen.generate(schema_with_char_field) produces source containing "class ResPartner(OdooBaseModel):"
    - gen.generate(schema) produces source containing '__odoo_model__: ClassVar[str] = "res.partner"' (with double quotes, per repr() convention for model names)
    - gen.generate(schema_with_id_field) produces source containing "    id: int" (plain int, no Optional, no default)
    - gen.generate(schema_with_char_field) produces source containing "    name: Optional[str] = None"
    - gen.generate(schema_with_bool_field) produces source containing "    active: bool = False"
    - gen.generate(schema_with_selection_field) produces source containing "Optional[Literal[" and "= None"
    - gen.generate(schema_with_many2one_in_set) produces source containing "Optional[Ref[ResCountry]]" and "from .res_country import ResCountry"
    - gen.generate(schema_with_many2one_not_in_set) produces source containing "Optional[Ref[int]]" and "# res.company" comment
    - compile(gen.generate(any_schema), "<test>", "exec") does not raise SyntaxError
    - importlib roundtrip: loaded module has ResPartner.__odoo_model__ == "res.partner"
    - source does NOT contain "FieldMeta", "TypedDict", "markers", "NotRequired", "Required"
    - gen.write([schema], tmp_path) writes res_partner.py and __init__.py
    - gen.write([schema], Path("/nonexistent")) raises ValueError matching "not a directory"
  </behavior>

  <action>
    Replace codegen.py. Keep: module docstring (update to "Pydantic model emitter"), from __future__ import annotations, logging import, TYPE_CHECKING block (keep importing Introspector, ModelSchema from introspector/types under TYPE_CHECKING), logger line, _model_to_classname verbatim (lines 50-52), _model_to_filename verbatim (lines 55-57), write() method structure (lines 185-220) — only change: call self.generate(schema) without extra args (in_set is on the instance), and update the barrel's note comment. Delete entirely: _FIELD_META_DEFAULTS dict, _annotated_field_meta_str function, old TypedDict generate() body.

    New top-level import (runtime, not TYPE_CHECKING): from godoo.introspection.type_mapper import pydantic_field_str

    New CodeGenerator.__init__ signature:
      def __init__(self, introspector: Introspector, in_set: frozenset[str] = frozenset()) -> None:
        self._introspector = introspector
        self._in_set = in_set

    New generate(self, schema: ModelSchema) -> str:
    Builds the file string for one Pydantic model. The generate() method assembles imports dynamically per model — it tracks which imports are needed as it processes fields. Import assembly logic:
    - Always: "from __future__ import annotations", "from typing import ClassVar, Optional", "from godoo.client._pydantic_transform import OdooBaseModel"
    - Add "from datetime import date" if any date fields; "from datetime import datetime" if any datetime fields (can combine: "from datetime import date, datetime")
    - Add "Literal" to typing import line if any static-selection fields
    - Add "Any" to typing import line if any json/properties/unknown-ttype fields
    - Add "from godoo.client.typed import Ref" if any many2one fields
    - Add per in-set many2one target: "from .{stem} import {classname}" as regular import, OR use TYPE_CHECKING guard for the alphabetically-second file in a circular pair

    Circular-import detection: for each many2one field where the target is in self._in_set, check if the target model also has a many2one field pointing back to this schema's model. Since generate() only has the current schema and in_set (not all schemas), use a simpler heuristic from RESEARCH.md Pattern 4: always emit regular imports (not TYPE_CHECKING) in the first pass. Add a note in the docstring that circular pairs would require a second pass to detect; for Phase 7 emit all in-set relations as regular imports (they are safe because from __future__ import annotations defers all annotation evaluation, so circular regular imports are also safe at class-definition time).

    File structure (emit as a list of lines, join with "\n"):
    1. Header comment block: "# AUTOGENERATED by godoo-introspection - do not edit manually.", f"# Model: {schema.name}", ""
    2. "from __future__ import annotations", ""
    3. stdlib imports (date/datetime — only if needed, combined into one line), (blank line if stdlib section non-empty)
    4. typing imports: assemble as "from typing import ClassVar, Optional" + optional Literal, Any; always on one line
    5. ""
    6. "from godoo.client._pydantic_transform import OdooBaseModel"
    7. "from godoo.client.typed import Ref"  — only if any many2one field
    8. "" (blank before cross-imports if any)
    9. One per in-set many2one target: "from .{_model_to_filename(target)[:-3]} import {_model_to_classname(target)}"
    10. "" (blank line before class), "" (second blank line)
    11. f"class {class_name}(OdooBaseModel):"
    12. f'    __odoo_model__: ClassVar[str] = {schema.name!r}'  — use !r so "res.partner" becomes '"res.partner"' (double quotes via repr)
    13. ""
    14. "    id: int"  — always emitted first; no Optional, no default
    15. Per non-id field (sorted by field name, skipping fields failing isidentifier() check with warning):
        annotation, default = pydantic_field_str(field, self._in_set, _model_to_classname)
        Emit: f"    {field_name}: {annotation} = {default}"
    16. If no non-id fields: emit "    pass" (after id: int)
    17. ""  (trailing newline)

    Preserve identifier validation guard from codegen.py line 170-172 in the new field loop.

    Replace test_codegen.py: preserve _schema(), _char_field() helpers; preserve test_model_to_classname(), test_model_to_filename(), test_write_creates_files(), test_write_creates_init(), test_write_invalid_dir_raises(). Delete 6 TypedDict-specific tests (test_generate_class_name asserts TypedDict, test_generate_id_required asserts Required[int], test_generate_field_notrequired asserts NotRequired[, test_generate_field_meta_import asserts markers import, test_generate_selection_literal old form, test_generate_header old from typing import form). Replace test_generate_get_type_hints_round_trip with a Pydantic-flavored importlib roundtrip that asserts __odoo_model__ == "res.partner". Add new Pydantic-specific tests per PATTERNS.md §tests/test_codegen.py: test_generate_pydantic_class_name, test_generate_odoo_model_classvar, test_generate_id_plain_int, test_generate_optional_char, test_generate_bool_non_optional, test_generate_no_field_meta. Add a test for many2one in-set cross-import and many2one not-in-set degraded comment.

    NOTE: CodeGenerator(None) constructor still works for tests (introspector not needed for generate()).
  </action>

  <verify>
    <automated>uv run pytest packages/godoo-introspection/tests/test_codegen.py -v && uv run ruff check packages/godoo-introspection/src packages/godoo-introspection/tests && uv run mypy packages/godoo-introspection/src</automated>
  </verify>

  <acceptance_criteria>
    - All new test_codegen.py tests pass
    - compile(gen.generate(any_schema), "<test>", "exec") does not raise (smoke test passes)
    - importlib roundtrip: loaded ResPartner has __odoo_model__ == "res.partner" and is a subclass of OdooBaseModel
    - source does NOT contain "TypedDict", "NotRequired", "Required", "FieldMeta", "markers"
    - source DOES contain "class ResPartner(OdooBaseModel):" and '__odoo_model__: ClassVar[str] = "res.partner"'
    - "    id: int" is present with no Optional and no default expression
    - boolean field active emits "    active: bool = False"
    - char field name emits "    name: Optional[str] = None"
    - many2one in-set emits "from .res_country import ResCountry" in the imports section
    - many2one not-in-set emits "Optional[Ref[int]]" and trailing "# res.company" comment
    - write() test writes res_partner.py and __init__.py to tmp_path (preserved behavior)
    - uv run pytest packages/ -m "not integration" passes (all unit tests green)
    - uv run ruff check packages/godoo-introspection/src packages/godoo-introspection/tests passes
    - uv run mypy packages/godoo-introspection/src passes (strict)
  </acceptance_criteria>

  <done>codegen.py replaced with Pydantic emitter; test_codegen.py rewritten; full unit test suite green; ruff + mypy pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| field name from FieldSchema → generated source | Field names from Odoo schema are embedded in generated Python source; must not inject arbitrary Python syntax |
| field.selection values → generated Literal | Odoo selection value strings are embedded as repr'd Literal values in generated source |
| schema.name → generated ClassVar and class name | Odoo model names are used to construct class names and ClassVar[str] values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | codegen.py generate() — field name embedding | mitigate | Existing `field_name.isidentifier()` guard preserved verbatim from codegen.py:170; non-identifier fields are skipped with a warning |
| T-07-02 | Tampering | codegen.py generate() — selection value embedding | mitigate | `repr(v)` for each selection value (per PATTERNS.md shared pattern) ensures special characters and quote sequences cannot break out of the Literal annotation string |
| T-07-03 | Tampering | codegen.py generate() — model name in ClassVar | mitigate | `schema.name!r` (repr) in the ClassVar line; model names from Odoo are dot-separated alphanumeric identifiers but repr() provides a safe boundary regardless |
| T-07-04 | Information Disclosure | test files — no credentials | accept | No credentials involved in Plan 01; test fixtures use constants (http://odoo.test, admin/admin) with no real secrets |
| T-07-SC | Tampering | pydantic>=2.13 / typer>=0.26 installs | accept | Package Legitimacy Audit in 07-RESEARCH.md confirmed both packages [OK] — pydantic (7yr, 100M+/wk downloads) and typer (6yr, 10M+/wk downloads); no postinstall scripts |
</threat_model>

<verification>
Run the full quality gate after all three tasks:

```
uv run ruff check . && uv run ruff format . && uv run mypy packages/godoo-client/src packages/godoo-testcontainers/src packages/godoo-introspection/src && uv run pytest packages/ -m "not integration"
```

All must pass. Specifically verify:
- markers.py absent: python -c "import godoo.introspection.markers" raises ModuleNotFoundError
- FieldMeta absent from public API: python -c "from godoo.introspection import FieldMeta" raises ImportError
- Package still importable: python -c "from godoo.introspection import CodeGenerator, FieldSchema, Introspector, IntrospectionCache, ModelSchema"
- Pydantic emitter compiles generated code without SyntaxError for a representative schema
</verification>

<success_criteria>
1. markers.py deleted; FieldMeta not exported from godoo.introspection
2. type_mapper.py exports pydantic_field_str(field, in_set, classname_fn) -> tuple[str, str]; python_type_str is gone
3. codegen.py CodeGenerator.__init__ accepts in_set: frozenset[str] = frozenset(); generate() emits Pydantic OdooBaseModel subclasses
4. Generated files: from __future__ import annotations, OdooBaseModel subclass, __odoo_model__: ClassVar[str], id: int, Optional[T] = None for scalars, bool = False for boolean, Literal for static selection, Ref[TargetClass] or Ref[int] for many2one, list[int] for x2many
5. test_type_mapper.py: all tests pass against Pydantic type forms; no old TypedDict assertions
6. test_codegen.py: all tests pass against Pydantic emitter; compile() + importlib roundtrip green
7. test_introspector.py: FieldMeta tests deleted; remaining 8 tests pass
8. ruff check . passes; mypy packages/godoo-introspection/src passes (strict); pytest packages/ -m "not integration" passes
</success_criteria>

<output>
Create `.planning/phases/07-pydantic-cli-generator/07-01-SUMMARY.md` when done.
</output>
