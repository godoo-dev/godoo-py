---
phase: 08-pyodide-spike
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - .planning/phases/08-pyodide-spike/08-SPIKE.md
  - spikes/08-pyodide/index.html
autonomous: false
requirements: [BROWSER-02, BROWSER-03]
must_haves:
  truths:
    - "The throwaway ACA Odoo endpoint is deployed and reachable over its managed-cert HTTPS *.azurecontainerapps.io FQDN"
    - "An actual Odoo JSON-RPC call (not an import check) is executed from within the Pyodide runtime, cross-origin, over HTTPS — proven by a captured non-error result"
    - "All three D-06 strategies were run in isolation and each strategy's worked/failed status + error output (for failures) is recorded"
    - "A Python-floor recommendation (Option A defer-to-3.14 vs Option B drop-to-3.12) is documented with rationale"
    - "D-07: the transport is exercised in isolation against /jsonrpc (never `import godoo`, which is >=3.14-gated and won't load under Pyodide CPython 3.13); the Python floor stays an output recommendation in 08-SPIKE.md, not something the spike engineers around"
  artifacts:
    - path: ".planning/phases/08-pyodide-spike/08-SPIKE.md"
      provides: "The written verdict: per-strategy results table + captured evidence + Python-floor recommendation"
      contains: "Strategy"
  key_links:
    - from: "spikes/08-pyodide/index.html"
      to: "the deployed *.azurecontainerapps.io endpoint"
      via: "in-browser pyfetch/httpx POST to /jsonrpc over HTTPS"
      pattern: "azurecontainerapps.io"
    - from: ".planning/phases/08-pyodide-spike/08-SPIKE.md"
      to: "spikes/08-pyodide/index.html run output"
      via: "pasted per-strategy tracebacks/results + devtools Network evidence"
      pattern: "Strategy [123]"
---

<objective>
Deploy the throwaway Azure endpoint (Plan 02 Bicep), fill the spike page placeholders (Plan 01),
run the raw Pyodide page in a real browser against the live HTTPS endpoint, and capture the
per-strategy evidence into `08-SPIKE.md` — the written verdict. This is the empirical heart of
the spike: it produces the SC-1 cross-origin HTTPS call, the SC-2 per-strategy worked/failed
table, and the SC-3 Python-floor recommendation.

Purpose: SC-1 cannot be satisfied without a live TLS-terminated cloud Odoo; this plan runs the
real call and records what actually happens (the strategy-1 traceback, the strategy-2 brittle
success, the strategy-3 maintainable success — or whatever the browser actually emits).

Output: `.planning/phases/08-pyodide-spike/08-SPIKE.md` (evidence + Python-floor rec) and the
filled-in `spikes/08-pyodide/index.html` (endpoint/creds wired for the run; commit with the
real FQDN but NO committed secret).

This plan is NOT autonomous: it requires a live `az deployment` (an active Azure subscription /
`az login`) and a human-driven in-browser run (loading the page, observing the call in devtools).
execute-phase surfaces it for manual run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pyodide-spike/08-CONTEXT.md
@.planning/phases/08-pyodide-spike/08-RESEARCH.md
@.planning/phases/08-pyodide-spike/08-01-SUMMARY.md
@.planning/phases/08-pyodide-spike/08-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy the throwaway ACA endpoint and run the in-browser spike</name>
  <files>spikes/08-pyodide/index.html</files>
  <read_first>
    - spikes/08-pyodide/infra/deploy.md (the az deploy sequence + FQDN handoff + manual teardown backstop)
    - spikes/08-pyodide/infra/main.bicep and selfdestruct.bicep (what is being deployed)
    - spikes/08-pyodide/index.html and README.md (the placeholders to fill + the real-origin serving requirement)
    - 08-RESEARCH.md §"Environment Availability" (az login + active subscription is the Wave-0 precondition; no local fallback for SC-1) and §"Common Pitfalls" Pitfall 2 (verify CORS in devtools Network tab BEFORE blaming the transport)
  </read_first>
  <what-built>
    Plans 01-02 produced: the raw Pyodide page (3-strategy runner), the PyfetchTransport prototype,
    and the ACA + self-destruct Bicep. This task deploys the Bicep and runs the page live.
  </what-built>
  <action>
    Manual procedure (no headless agent can run live `az deployment` or drive a browser): confirm
    `az login` + active subscription; create the dedicated RG and `az deployment group create` for
    `main.bicep` then `selfdestruct.bicep` (passing the page origin + throwaway creds as deploy-time
    params, never committed); read the `*.azurecontainerapps.io` FQDN and fill the
    `__FILL_AT_RUN_TIME__` placeholders in `index.html`; serve the page from a real origin; run all
    three strategies in the browser; capture per-strategy output + a devtools Network screenshot; then
    confirm RG teardown. Full step list in <how-to-verify>.
  </action>
  <how-to-verify>
    1. Confirm `az login` and an active subscription (precondition — no local fallback for SC-1).
    2. Create the dedicated RG and deploy per `deploy.md`: `main.bicep` then `selfdestruct.bicep`,
       passing the page origin + throwaway Odoo/Postgres creds as deploy-time params (NEVER commit
       the secret values).
    3. Read the resulting `*.azurecontainerapps.io` FQDN; fill the `__FILL_AT_RUN_TIME__` placeholders
       in `index.html` (endpoint base_url, db) — wire the throwaway creds via URL query params or a
       local working copy, NOT a committed secret.
    4. Serve the page from a REAL origin (GitHub Pages for HTTPS-to-HTTPS, or `python -m http.server`
       for a localhost origin — NOT `file://`, whose Origin is `null` and CORS rejects).
    5. Open the page in a browser. Watch all three strategies run. In devtools Network, confirm the
       OPTIONS preflight returns the CORS headers (Pitfall 2 — verify CORS before blaming transport).
    6. Capture: a SUCCESSFUL `res.users` read result (SC-1 cross-origin HTTPS call), the strategy-1
       traceback, the strategy-2 result + resolved httpx version, the strategy-3 result, and a
       devtools Network screenshot. These paste into `08-SPIKE.md` (Task 2).
    7. Confirm the self-destruct fired (RG disappears after the TTL) OR run the manual
       `az group delete -n rg-godoo-pyodide-spike` backstop. NEVER leave the endpoint/creds live.
  </how-to-verify>
  <verify>
    <human-check>The operator confirms (resume-signal) a successful cross-origin HTTPS JSON-RPC result was observed in the browser and the per-strategy outputs + devtools Network screenshot are captured for Task 2; the RG is torn down.</human-check>
  </verify>
  <acceptance_criteria>
    - The ACA endpoint is reachable over its `*.azurecontainerapps.io` HTTPS FQDN
    - `index.html` has its endpoint/db placeholders filled with the real FQDN (committed); no secret committed
    - A successful cross-origin HTTPS JSON-RPC `res.users` result was observed in the browser (SC-1)
    - Per-strategy worked/failed output (traceback for failures, result for successes) + a devtools Network screenshot were captured for Task 2
    - The RG was torn down (self-destruct fired or manual `az group delete` run)
  </acceptance_criteria>
  <done>The endpoint was deployed, an actual cross-origin HTTPS JSON-RPC call ran from Pyodide with captured evidence, all three strategies were exercised in isolation, and the RG was torn down.</done>
  <resume-signal>Type "deployed and ran" with the captured per-strategy outputs + screenshot pasted/attached, or describe what blocked (e.g. az login missing, CORS preflight failed).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 08-SPIKE.md — per-strategy evidence + Python-floor recommendation</name>
  <files>.planning/phases/08-pyodide-spike/08-SPIKE.md</files>
  <read_first>
    - The captured run outputs from Task 1 (per-strategy tracebacks/results + devtools Network screenshot)
    - 08-RESEARCH.md §"Validation Architecture" (the per-strategy evidence-capture table: SC-1 successful result, SC-2 3-row worked/failed table with verbatim tracebacks, SC-3 Python Floor section)
    - 08-RESEARCH.md §"Python-Floor Reality (D-07, BROWSER-03)" (Option A defer-to-3.14 recommended default vs Option B drop-to-3.12; Pyodide ships CPython 3.13; PEP 776/783 Emscripten tier-3 from 3.14)
    - 08-RESEARCH.md §"OD-3 Resolution" (expected shape: stock httpx fails, pyodide-httpx works-but-brittle, custom transport works-maintainable) and the /jsonrpc-deprecation-in-Odoo-22 one-line note
    - 08-CONTEXT.md D-08 (evidence → 08-SPIKE.md; decision → ADR) and D-10 (the go bar)
  </read_first>
  <action>
    Write `08-SPIKE.md` as the written verdict (D-08 — evidence only; the durable go/no-go decision
    is the Plan-04 ADR). Include: a one-line spike summary; a SC-1 evidence block (the captured
    successful cross-origin HTTPS `res.users` result + the devtools Network OPTIONS/CORS evidence,
    proving an ACTUAL JSON-RPC call, not an import check); a SC-2 three-row per-strategy table
    (Strategy 1 stock httpx / Strategy 2 pyodide-httpx==0.2.0 / Strategy 3 custom PyfetchTransport)
    with columns worked|failed and an error-output column carrying the VERBATIM traceback for
    failures and the resolved httpx version for strategy 2; and a SC-3 "Python Floor" section
    recommending Option A (defer a shipped browser build until Pyodide ships CPython >=3.14 —
    keeps the single >=3.14 monorepo floor; aligns with PEP 776/783 Emscripten-tier-3-from-3.14)
    or Option B (drop `requires-python` to >=3.12 for a browser-specific build — ships now, divergent
    floor; note pyodide-httpx is itself >=3.12) with the rationale, framed so the verdict author picks.
    Add the one-line note that `/jsonrpc` is deprecated for removal in Odoo 22 (fall 2028) as a
    longer-term consideration. Record whether the D-10 go bar is met (at least one strategy that BOTH
    makes the cross-origin HTTPS call AND is maintainable to ship) but leave the formal go/no-go
    DECISION to the ADR (Plan 04). Do NOT paste any secret (creds) into the evidence.
  </action>
  <acceptance_criteria>
    - `.planning/phases/08-pyodide-spike/08-SPIKE.md` has a per-strategy results table with worked/failed columns AND an error-output column populated from the real run (verbatim tracebacks for failures)
    - A SC-1 evidence block records the captured successful cross-origin HTTPS JSON-RPC result + the devtools Network/CORS evidence
    - A "Python Floor" section documents Option A vs Option B with rationale (BROWSER-03 SC-3)
    - The D-10 go-bar assessment is stated; the formal decision is deferred to the ADR
    - No secret/credential value appears anywhere in the file
  </acceptance_criteria>
  <verify>
    <automated>Static check: `grep -Ec "Strategy 1|Strategy 2|Strategy 3" .planning/phases/08-pyodide-spike/08-SPIKE.md` >= 3 AND `grep -Eic "python floor|requires-python" .planning/phases/08-pyodide-spike/08-SPIKE.md` >= 1 AND `grep -Ec "3\.14|3\.12" .planning/phases/08-pyodide-spike/08-SPIKE.md` >= 1.</automated>
  </verify>
  <done>08-SPIKE.md records the SC-1 successful call evidence, the SC-2 per-strategy table with verbatim error output, and the SC-3 Python-floor recommendation — with no committed secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Azure control plane | live `az deployment` provisions a publicly-reachable endpoint |
| browser → live Odoo endpoint | the in-browser cross-origin HTTPS call carries throwaway creds in the body |
| captured evidence → committed SPIKE.md | run output is pasted into a committed public doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | committed index.html (filled) / 08-SPIKE.md | mitigate | Commit the real FQDN but NEVER a credential value; creds are passed via URL params / local working copy at run time. Strip any cred from pasted evidence before committing (secrets-handling rule). |
| T-08-09 | Denial-of-control | endpoint/creds linger if self-destruct fails | mitigate | Task 1 step 7 confirms RG teardown (self-destruct fired OR manual `az group delete`); never leave the endpoint live. Short TTL bounds exposure. |
| T-08-10 | Spoofing | throwaway Odoo admin login reachable for the window | accept | Throwaway DB with no real data; throwaway password never reused; short TTL + self-destruct. Residual risk accepted for the spike window (ASVS V2). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | The only installs are the Plan-01 in-page micropip (`pyodide-httpx==0.2.0`, `httpx`, `ssl`), never added to a project manifest (D-06). No pyproject change in this plan. |
</threat_model>

<verification>
- The ACA endpoint was deployed and reachable over HTTPS; a successful cross-origin JSON-RPC call was observed (SC-1).
- `08-SPIKE.md` carries the per-strategy worked/failed table with verbatim error output (SC-2) and the Python-floor recommendation (SC-3).
- The RG was torn down; no credential value is committed anywhere.
</verification>

<success_criteria>
- SC-1: an actual cross-origin HTTPS JSON-RPC call ran from Pyodide against a real TLS-terminated Odoo endpoint, with captured evidence.
- SC-2: all three strategies' worked/failed status + error output are recorded.
- SC-3: a Python-floor recommendation with rationale is documented.
</success_criteria>

<output>
Create `.planning/phases/08-pyodide-spike/08-03-SUMMARY.md` when done
</output>
