---
phase: 08-pyodide-spike
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - docs/adr/0001-pyodide-browser-go-no-go.md
  - mkdocs.yml
autonomous: true
requirements: [BROWSER-03]
must_haves:
  truths:
    - "A standalone MADR-style ADR records an explicit go OR no-go OR conditional decision on a browser build, applying the D-10 go bar to the SPIKE.md results"
    - "The ADR states the v2.0-escalation consequence (a 'go' with required breaking changes escalates to v2.0; a 'no-go' defers BROWSER-F1/F2 to the backlog)"
    - "The ADR is reachable from the published docs site via an mkdocs nav entry"
  artifacts:
    - path: "docs/adr/0001-pyodide-browser-go-no-go.md"
      provides: "The durable go/no-go decision the fleet / v2.0 planning reads (D-08)"
      contains: "Decision"
    - path: "mkdocs.yml"
      provides: "ADR nav section so the decision ships in the docs site"
      contains: "ADR"
  key_links:
    - from: "docs/adr/0001-pyodide-browser-go-no-go.md"
      to: ".planning/phases/08-pyodide-spike/08-SPIKE.md"
      via: "the ADR references the SPIKE doc for the per-strategy evidence"
      pattern: "08-SPIKE"
    - from: "mkdocs.yml"
      to: "docs/adr/0001-pyodide-browser-go-no-go.md"
      via: "nav entry"
      pattern: "adr/0001"
---

<objective>
Record the durable go/no-go decision as a standalone MADR-style ADR (D-08 — separating the
fleet-readable DECISION from the SPIKE.md EVIDENCE) and wire it into the docs site nav. This
establishes the `docs/adr/` convention (none exists today) and delivers the SC-4 explicit
go/no-go decision that v2.0 planning reads directly.

Purpose: D-08 deliberately splits evidence (08-SPIKE.md, Plan 03) from the decision (this ADR).
The ADR applies the D-10 go bar to the recorded strategy results and states the v2.0-escalation
consequence — it is what gates BROWSER-F1/F2 out of the backlog.

Output: `docs/adr/0001-pyodide-browser-go-no-go.md` (status `accepted`) + an `ADR` nav section in
`mkdocs.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pyodide-spike/08-CONTEXT.md
@.planning/phases/08-pyodide-spike/08-RESEARCH.md
@.planning/phases/08-pyodide-spike/08-SPIKE.md
@.planning/phases/08-pyodide-spike/08-03-SUMMARY.md
@mkdocs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the standalone go/no-go ADR (MADR-style)</name>
  <files>docs/adr/0001-pyodide-browser-go-no-go.md</files>
  <read_first>
    - .planning/phases/08-pyodide-spike/08-SPIKE.md (the per-strategy results + Python-floor recommendation this ADR decides from)
    - 08-RESEARCH.md §"ADR Convention (D-08)" (MADR-style at docs/adr/0001-...md; status accepted; context/decision/consequences; references the SPIKE doc; assumption A1 — flag if Marc prefers .planning/adrs/)
    - 08-RESEARCH.md §"Python-Floor Reality" (the Option A/B framing the decision adopts) and §"OD-3 Resolution" (the empirical basis)
    - 08-CONTEXT.md D-08 (decision → standalone ADR), D-10 (the go bar: at least one strategy that BOTH makes the cross-origin HTTPS call AND is maintainable to ship; brittle-only = conditional/no-go)
    - .planning/STATE.md "Deferred Items" (BROWSER-F1 godoo[browser] extra, BROWSER-F2 relax Python floor — both gated on this go verdict)
  </read_first>
  <action>
    Write `docs/adr/0001-pyodide-browser-go-no-go.md` as a MADR-style ADR (sections: Title,
    Status, Context, Decision, Consequences). Set Status to `accepted`. In Context, summarize the
    spike question (can godoo run an Odoo JSON-RPC call from Pyodide/browser?) and reference
    `.planning/phases/08-pyodide-spike/08-SPIKE.md` for the per-strategy evidence (do NOT duplicate
    the full tracebacks — link to them). In Decision, apply the D-10 go bar to the recorded results:
    a "go" requires at least one strategy that BOTH made the cross-origin HTTPS call AND is
    maintainable/sustainable to ship (the custom PyfetchTransport via the existing `transport_factory`
    seam is the candidate; a success that ONLY worked via the brittle `pyodide-httpx` patch is recorded
    as conditional/no-go). State the explicit verdict (go / no-go / conditional) and the adopted
    Python-floor recommendation (Option A defer-to-3.14 default, or Option B drop-to-3.12) carried
    from 08-SPIKE.md. In Consequences, state: a "go" with required breaking changes escalates the
    browser work to v2.0 planning; a "no-go" defers BROWSER-F1 (`godoo[browser]` extra) and BROWSER-F2
    (relax Python floor) to the backlog; the non-shipping PyfetchTransport prototype seeds BROWSER-F1
    if "go". Note this ADR establishes the `docs/adr/` convention (A1 — first ADR in the repo).
  </action>
  <acceptance_criteria>
    - `docs/adr/0001-pyodide-browser-go-no-go.md` has MADR sections: Status (`accepted`), Context, Decision, Consequences
    - The Decision section records an EXPLICIT go OR no-go OR conditional verdict, applying the D-10 bar
    - The adopted Python-floor recommendation (Option A or B) is carried into the ADR
    - The Consequences section states the v2.0-escalation path and the BROWSER-F1/F2 backlog-defer
    - The ADR references `08-SPIKE.md` for evidence rather than duplicating the tracebacks
  </acceptance_criteria>
  <verify>
    <automated>Static check: `grep -Eic "^#*\s*(status|decision|consequences|context)" docs/adr/0001-pyodide-browser-go-no-go.md` >= 4 AND `grep -Eic "\bgo\b|no-go|conditional" docs/adr/0001-pyodide-browser-go-no-go.md` >= 1 AND `grep -c "08-SPIKE" docs/adr/0001-pyodide-browser-go-no-go.md` >= 1.</automated>
  </verify>
  <done>The ADR records an explicit go/no-go decision applying the D-10 bar, carries the Python-floor recommendation, states the v2.0/backlog consequences, and references the SPIKE evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add the ADR to the mkdocs nav</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (the existing `nav:` block — Home / Getting Started / Services / Guides / API Reference / Testing; add a new ADR section without disturbing the others)
    - 08-RESEARCH.md §"ADR Convention (D-08)" (add an ADR section to mkdocs nav so v2.0 planning + users can read it; docs/ is already the mkdocs source root)
  </read_first>
  <action>
    Add an `ADR` section to the `nav:` block in `mkdocs.yml` with a single entry pointing at
    `adr/0001-pyodide-browser-go-no-go.md` (paths in nav are relative to the docs source root, so
    use `adr/0001-pyodide-browser-go-no-go.md`, NOT `docs/adr/...`). Place it after `Testing`
    (or another sensible position) without altering existing nav entries, the `plugins`,
    `markdown_extensions`, or `theme` blocks. Do not change the mkdocstrings paths.
  </action>
  <acceptance_criteria>
    - `mkdocs.yml` `nav:` contains an `ADR` section referencing `adr/0001-pyodide-browser-go-no-go.md`
    - The nav path is docs-root-relative (`adr/0001-...`, not `docs/adr/0001-...`)
    - Existing nav entries, plugins, markdown_extensions, and mkdocstrings paths are unchanged
  </acceptance_criteria>
  <verify>
    <automated>`grep -c "adr/0001-pyodide-browser-go-no-go.md" mkdocs.yml` >= 1 AND (if mkdocs available) `mkdocs build --strict` succeeds; otherwise confirm YAML parses: `python -c "import yaml,sys; yaml.safe_load(open('mkdocs.yml'))"` exits 0.</automated>
  </verify>
  <done>The go/no-go ADR is reachable from the published docs site via an mkdocs nav entry; existing nav is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR → public docs site | the decision ships in the published mkdocs site (world-readable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Disclosure | docs/adr/0001 (public ADR) | mitigate | The ADR records a decision + references the SPIKE evidence; it carries NO credential, FQDN, or secret. Confidentiality rule: no client/firm names — this is a technical decision doc only. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | This plan modifies only docs/markdown and mkdocs.yml nav — no package-manager install, no dependency manifest change. |
</threat_model>

<verification>
- `docs/adr/0001-pyodide-browser-go-no-go.md` exists with MADR sections and an explicit verdict.
- `mkdocs.yml` nav references the ADR (docs-root-relative path); existing nav intact.
- No secret/FQDN/credential or confidential name appears in the ADR.
</verification>

<success_criteria>
- SC-4: an explicit go/no-go decision is recorded in a standalone, fleet-readable ADR that v2.0 planning reads directly, with the v2.0-escalation / BROWSER-F1/F2-defer consequences stated.
- The `docs/adr/` convention is established and wired into the docs site.
</success_criteria>

<output>
Create `.planning/phases/08-pyodide-spike/08-04-SUMMARY.md` when done
</output>
