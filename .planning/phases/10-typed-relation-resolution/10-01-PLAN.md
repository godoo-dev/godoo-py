---
phase: 10-typed-relation-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/godoo-client/src/godoo/client/typed.py
  - packages/godoo-client/src/godoo/client/_pydantic_transform.py
  - packages/godoo-client/tests/test_typed_dispatch.py
autonomous: true
requirements:
  - REL-01
  - TEST-02

must_haves:
  truths:
    - "Ref(id=1, name='X') == Ref(id=1, name='X') still holds after adding _target_cls (compare=False preserves equality)"
    - "Ref(id=1, name='X') hash is unchanged — existing frozen-dataclass hash semantics intact"
    - "Wire transform populates _target_cls on Ref instances produced from m2o [id, 'Name'] tuples when the field annotation is Ref[SomeModel]"
    - "Wire transform leaves _target_cls=None when annotation is bare Ref or Ref[int]"
    - "test_typed_dispatch.py TEST-02 case passes: a TinyPartner with a Ref-typed parent_id field produces a Ref whose _target_cls is TinyPartner after client.read()"
  artifacts:
    - path: "packages/godoo-client/src/godoo/client/typed.py"
      provides: "_target_cls field on Ref[T] frozen dataclass"
      contains: "_target_cls: type | None = field(default=None, compare=False, hash=False, repr=False)"
    - path: "packages/godoo-client/src/godoo/client/_pydantic_transform.py"
      provides: "_ref_target_class() helper + updated m2o branch"
      contains: "_ref_target_class"
    - path: "packages/godoo-client/tests/test_typed_dispatch.py"
      provides: "TEST-02 wire-fidelity test"
      contains: "test_read_typed_ref_field_populated"
  key_links:
    - from: "_pydantic_transform.py _odoo_wire_transforms"
      to: "Ref constructor"
      via: "_ref_target_class(annotation) result passed as _target_cls= kwarg"
      pattern: "_target_cls=_ref_target_class"
    - from: "_pydantic_transform.py _ref_target_class"
      to: "get_args / get_origin"
      via: "existing typing imports at line 11"
      pattern: "get_args\\(annotation\\)"
---

<objective>
Add `_target_cls: type | None` field to `Ref[T]`, implement `_ref_target_class()` helper in `_pydantic_transform.py` to extract the type argument from a `Ref[T]` annotation, populate `_target_cls` in the m2o wire-transform branch, and extend `test_typed_dispatch.py` with a TEST-02 wire-fidelity test that verifies `_target_cls` is set correctly after a full `client.read()` dispatch.

Purpose: REL-01 — a typed `Ref[T]` must carry its target model class at runtime so the Phase 10 Wave 2 dispatcher can resolve it without the caller naming the model.

Output: Updated `typed.py`, updated `_pydantic_transform.py`, extended `test_typed_dispatch.py` with one new test.
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/marc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-typed-relation-resolution/10-CONTEXT.md
@.planning/phases/10-typed-relation-resolution/10-PATTERNS.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From packages/godoo-client/src/godoo/client/typed.py (lines 22-35):
Current Ref[T] — frozen dataclass, no _target_cls yet:
  @dataclass(frozen=True)
  class Ref[T]:
      id: int
      name: str | None
  __all__ = ["OdooModel", "Ref"]

  Current imports: from dataclasses import dataclass

From packages/godoo-client/src/godoo/client/_pydantic_transform.py:
  line 11: from typing import Any, ClassVar, get_args, get_origin
  line 38-49: _annotation_mentions_ref(annotation: Any) -> bool  -- do NOT change return type
  line 129-139: m2o transform branch that creates Ref(id=value[0], name=...) -- add _target_cls= kwarg here
  The function _ref_target_class does NOT yet exist -- add it as a new sibling after _annotation_mentions_ref.

From packages/godoo-client/tests/test_typed_dispatch.py (lines 47-52):
  class TinyPartner(OdooBaseModel):
      __odoo_model__: ClassVar[str] = "res.partner"
      id: int
      name: str | None = None
  Add: parent_id: Ref[TinyPartner] | None = None
  Import to add: from godoo.client.typed import Ref
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _target_cls field to Ref[T] and implement _ref_target_class() helper</name>
  <files>
    packages/godoo-client/src/godoo/client/typed.py,
    packages/godoo-client/src/godoo/client/_pydantic_transform.py
  </files>
  <read_first>
    packages/godoo-client/src/godoo/client/typed.py -- read full file before editing
    packages/godoo-client/src/godoo/client/_pydantic_transform.py -- read lines 1-55 (imports, helpers) and lines 125-145 (m2o branch)
    .planning/phases/10-typed-relation-resolution/10-PATTERNS.md -- §typed.py and §_pydantic_transform.py for exact field declaration and helper body
    .planning/phases/10-typed-relation-resolution/10-CONTEXT.md -- decisions D-01..D-04, carry-forward locks
  </read_first>
  <behavior>
    - Ref equality: Ref(id=1, name="X") == Ref(id=1, name="X") and Ref(id=1, name="X") == Ref(id=1, name="X", _target_cls=SomeClass) — compare=False ensures this
    - Ref hash: hash(Ref(id=1, name="X")) is unchanged regardless of _target_cls value
    - Ref repr: repr(Ref(id=1, name="X")) does NOT include _target_cls — repr=False ensures this
    - _ref_target_class(Ref[TinyPartner]): returns TinyPartner
    - _ref_target_class(Ref[TinyPartner] | None): returns TinyPartner (union unwrapping)
    - _ref_target_class(Ref): returns None (bare Ref, no type arg)
    - _ref_target_class(Ref[int]): returns None (int is not a type[OdooModel], but int IS a type — returns int; acceptable; dispatcher will find _target_cls lacks __odoo_model__ and the untyped-ref guard fires)
    - m2o branch: Ref created with _target_cls=_ref_target_class(annotation) kwarg added
  </behavior>
  <action>
    In typed.py:
    - Add `field` to the `from dataclasses import dataclass` import (becomes `from dataclasses import dataclass, field`).
    - Add `_target_cls: type | None = field(default=None, compare=False, hash=False, repr=False)` as the third field on `Ref[T]`, after `name: str | None`. Docstring can note "Runtime target class; excluded from equality, hash, and repr."
    - `__all__` stays `["OdooModel", "Ref"]` — no change.

    In _pydantic_transform.py:
    - Add `_ref_target_class(annotation: Any) -> type | None` as a new module-private function immediately after `_annotation_mentions_ref` (around line 51). Mirror its structure: check `get_origin(annotation) is Ref` first, then scan `get_args(annotation)` recursively. Return `args[0]` (the type param) when `args[0]` is a `type` instance; return `None` for bare `Ref`, `Ref[int]` where `int` is a type (return it as-is — let the dispatcher's untyped-ref guard handle non-OdooModel targets), or when no `Ref` is present. See PATTERNS.md §_pydantic_transform.py Injection point 1 for the exact body.
    - In the m2o transform branch at line 138 (`out[name] = Ref(id=value[0], name=...)`), add `_target_cls=_ref_target_class(annotation)` as a keyword arg to the `Ref(...)` constructor call. The existing `_annotation_mentions_ref` guard on line 136 stays unchanged — do NOT remove it; `_ref_target_class` is called only after the guard passes.
    - No other changes to `_pydantic_transform.py` — additive only.
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-client/tests/ -m "not integration" -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - typed.py contains `_target_cls: type | None = field(default=None, compare=False, hash=False, repr=False)` (grep: `grep "_target_cls" packages/godoo-client/src/godoo/client/typed.py`)
    - typed.py imports `field` from dataclasses: `from dataclasses import dataclass, field`
    - _pydantic_transform.py contains function `def _ref_target_class` (grep confirms)
    - _pydantic_transform.py m2o branch contains `_target_cls=_ref_target_class(annotation)` (grep confirms)
    - uv run mypy packages/godoo-client/src exits 0 (no new type errors introduced)
    - uv run pytest packages/godoo-client/tests/ -m "not integration" -x exits 0 (all existing tests pass)
    - Inline behavior: `Ref(id=1, name="X") == Ref(id=1, name="X", _target_cls=object)` evaluates True (compare=False)
    - Inline behavior: `repr(Ref(id=1, name="X", _target_cls=object))` does NOT contain "_target_cls" (repr=False)
  </acceptance_criteria>
  <done>
    Ref[T] carries _target_cls field with compare=False, hash=False, repr=False. Wire transform populates _target_cls from the field's Ref[T] annotation. All pre-existing unit tests pass. mypy --strict passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEST-02 — extend test_typed_dispatch.py with wire-fidelity test through full dispatch chain</name>
  <files>
    packages/godoo-client/tests/test_typed_dispatch.py
  </files>
  <read_first>
    packages/godoo-client/tests/test_typed_dispatch.py -- read full file before editing
    .planning/phases/10-typed-relation-resolution/10-PATTERNS.md -- §test_typed_dispatch.py pattern (lines 236-259)
    .planning/phases/10-typed-relation-resolution/10-CONTEXT.md -- decision D-04 (TEST-02 scope: wire-fidelity, stays in test_typed_dispatch.py)
  </read_first>
  <behavior>
    - After client.read(TinyPartner, [1]) where Odoo returns {"id": 1, "parent_id": [3, "Acme"]}: result[0].parent_id is a Ref instance with id=3, name="Acme", _target_cls=TinyPartner
    - This exercises the full dispatch chain (OdooClient.read → _odoo_wire_transforms → Ref constructor) not just model_validate in isolation
    - The _target_cls on the returned Ref is TinyPartner (the class referenced in `parent_id: Ref[TinyPartner] | None`)
  </behavior>
  <action>
    In test_typed_dispatch.py:
    - Add `from godoo.client.typed import Ref` to the imports block (after the existing imports from godoo.client).
    - On the `TinyPartner` class definition (lines 47-52), add field `parent_id: Ref[TinyPartner] | None = None` after `name`. Self-referential Ref annotation is valid because `from __future__ import annotations` is present.
    - Add a new test function `test_read_typed_ref_field_populated` in the "type[T] path — TYPED-03" section. The test: mock the jsonrpc route to return `[{"id": 1, "parent_id": [3, "Acme"]}]`, call `await auth_client.read(TinyPartner, [1])`, assert `result[0].parent_id is not None`, assert `result[0].parent_id.id == 3`, assert `result[0].parent_id.name == "Acme"`, assert `result[0].parent_id._target_cls is TinyPartner`.
    - Follow existing test decorator order: `@respx.mock` then `@pytest.mark.asyncio` (lines 60-61 pattern).
    - No other changes to existing tests — additive only.
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-client/tests/test_typed_dispatch.py -m "not integration" -x -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - test_typed_dispatch.py contains function `test_read_typed_ref_field_populated` (grep confirms)
    - test_typed_dispatch.py imports Ref: `from godoo.client.typed import Ref`
    - TinyPartner class now has `parent_id` field (grep: `grep "parent_id" packages/godoo-client/tests/test_typed_dispatch.py`)
    - uv run pytest packages/godoo-client/tests/test_typed_dispatch.py -m "not integration" exits 0
    - test_read_typed_ref_field_populated is collected and passes (visible in -v output)
    - uv run mypy packages/godoo-client/src exits 0 (src unchanged in this task, confirms task 1 mypy clean)
  </acceptance_criteria>
  <done>
    TEST-02 wire-fidelity case is implemented and passing: a Ref[TinyPartner]-typed field populated through the full client.read() dispatch chain carries _target_cls=TinyPartner.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wire data → Ref constructor | m2o tuple `[id, "Name"]` from Odoo JSON-RPC response reaches `_ref_target_class()` via the field annotation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `_ref_target_class()` annotation reflection | accept | Returns `None` for unrecognised annotations; `None` propagates to `_target_cls`; Phase 10 Wave 2 dispatcher's untyped-ref guard fires on `None`. No code execution path — annotation is a static type object, not user-supplied data. |
| T-10-02 | Information Disclosure | `Ref._target_cls` attribute exposure | accept | `repr=False` keeps the class out of logged `Ref` representations. No PII in the target class itself. |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | No new packages installed in this plan. |
</threat_model>

<verification>
After both tasks complete:

```bash
uv run ruff check . && uv run ruff format . && uv run mypy packages/godoo-client/src packages/godoo-testcontainers/src packages/godoo-introspection/src
uv run pytest packages/godoo-client/tests/ -m "not integration" -x -q
```

Both commands must exit 0.
</verification>

<success_criteria>
- `Ref` dataclass gains `_target_cls: type | None = field(default=None, compare=False, hash=False, repr=False)` — equality/hash/repr semantics unchanged (REL-01).
- `_ref_target_class()` helper in `_pydantic_transform.py` extracts `T` from `Ref[T]` annotations, returning `None` for bare `Ref` or union-less annotations.
- m2o wire transform populates `_target_cls` via `_ref_target_class(annotation)` kwarg (REL-01).
- TEST-02 test in `test_typed_dispatch.py` asserts `result[0].parent_id._target_cls is TinyPartner` after a full `client.read()` dispatch (TEST-02).
- Full unit test suite passes; mypy --strict passes; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/10-typed-relation-resolution/10-01-SUMMARY.md` when done.
</output>
