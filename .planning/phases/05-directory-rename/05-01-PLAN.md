---
phase: 05-directory-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/godoo-client/  # (renamed from packages/godoo/ via git mv — all contents move)
  - pyproject.toml
  - .github/workflows/test.yml
  - mkdocs.yml
  - CONTRIBUTING.md
  - CLAUDE.md
  - uv.lock  # auto-regenerated by uv sync — not hand-edited
autonomous: true
requirements:
  - PKG-01
  - PKG-02
must_haves:
  truths:
    - "packages/godoo-client/ exists on disk; packages/godoo/ does not exist"
    - "git log --follow packages/godoo-client/src/godoo/client/client.py shows history predating the rename (blame preserved)"
    - "uv sync completes without error; uv run python -c 'import godoo.client' exits 0"
    - "(D-01) grep for bare packages/godoo (not followed by -client/-testcontainers/-introspection/-meta) returns zero hits in tracked files"
    - "(D-02) root pyproject.toml build_command no longer contains uv build --package godoo"
  artifacts:
    - path: "packages/godoo-client/pyproject.toml"
      provides: "Package manifest at new location (moved, not modified)"
    - path: "pyproject.toml"
      provides: "Updated mypy_path, version_toml, build_command"
    - path: ".github/workflows/test.yml"
      provides: "Updated mypy invocation path"
    - path: "mkdocs.yml"
      provides: "Updated mkdocstrings paths"
    - path: "CONTRIBUTING.md"
      provides: "Updated developer command references"
    - path: "CLAUDE.md"
      provides: "Updated structure and run-command references"
    - path: "uv.lock"
      provides: "Regenerated editable path entries (was packages/godoo, now packages/godoo-client)"
  key_links:
    - from: "pyproject.toml [tool.mypy] mypy_path"
      to: "packages/godoo-client/src"
      via: "direct path string update"
      pattern: "packages/godoo-client/src"
    - from: ".github/workflows/test.yml mypy step"
      to: "packages/godoo-client/src"
      via: "direct path string update"
      pattern: "packages/godoo-client/src"
    - from: "uv.lock editable entries"
      to: "packages/godoo-client"
      via: "uv sync auto-regeneration"
      pattern: "editable = .packages/godoo-client."
---

<objective>
Rename the workspace directory `packages/godoo/` to `packages/godoo-client/` using `git mv` (blame-preserving), then update every hardcoded `packages/godoo/` path reference across tool configs and documentation, and regenerate `uv.lock` via `uv sync`. The rename + reference updates are committed atomically so no intermediate CI state exists with broken paths.

Purpose: Align the on-disk directory name with the already-authoritative PyPI dist name (`godoo-client`) and the existing `[tool.uv.sources]` workspace alias. The Python import namespace (`godoo.*`) is untouched.

Output: `packages/godoo-client/` with full git history; all tool configs, CI workflow, docs, and `CLAUDE.md` updated; `uv.lock` regenerated; zero stale `packages/godoo/` references in tracked files.
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/marc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-directory-rename/05-RESEARCH.md
@.planning/phases/05-directory-rename/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv rename + uv sync lock regeneration</name>
  <files>packages/godoo-client/ (renamed from packages/godoo/), uv.lock</files>

  <read_first>
    - packages/godoo/pyproject.toml — confirm project.name = "godoo-client"; confirm directory structure before moving
    - uv.lock lines 271, 288, 301 — confirm current editable path values (should be packages/godoo) before running uv sync
    - packages/godoo-testcontainers/pyproject.toml — confirm dependencies uses "godoo-client>=0.1.0" (dist name, not path)
    - packages/godoo-introspection/pyproject.toml — same dist-name check
  </read_first>

  <action>
    Run git mv to rename the directory with history preservation:
      git mv packages/godoo packages/godoo-client

    Verify the move landed correctly (packages/godoo/ must not exist):
      ls packages/

    Regenerate uv.lock from the new path — do NOT hand-edit uv.lock:
      uv sync

    uv sync will rescan workspace members (by project.name in each pyproject.toml, not directory name), find packages/godoo-client/pyproject.toml with project.name = "godoo-client", and rewrite the three editable path entries at the old lines 271, 288, 301 from "packages/godoo" to "packages/godoo-client".

    Confirm uv.lock no longer contains the old path:
      grep "packages/godoo" uv.lock   (must return zero matches on the bare form; only "packages/godoo-client" should appear)

    Do NOT commit yet — Task 2 updates tool-config references and the commit covers both atomically.
  </action>

  <verify>
    <automated>ls packages/ | grep -E "^godoo$" | wc -l</automated>
    Output must be 0 (old directory gone). packages/godoo-client/ exists. uv sync exits 0. grep "editable.*packages/godoo\"" uv.lock returns 0 lines.
  </verify>

  <acceptance_criteria>
    - packages/godoo/ does not exist (ls packages/ shows no bare godoo/ entry)
    - packages/godoo-client/ exists and contains pyproject.toml, src/, tests/
    - git log --follow packages/godoo-client/src/godoo/client/client.py shows at least one commit predating the rename (history preserved via git mv)
    - uv sync exits 0 without workspace-member-not-found errors
    - grep for the string "packages/godoo\"" in uv.lock returns 0 matches (old editable path gone)
  </acceptance_criteria>

  <done>packages/godoo-client/ exists with full blame history; uv.lock regenerated with new paths; packages/godoo/ deleted</done>
</task>

<task type="auto">
  <name>Task 2: Update all path references in tool configs and documentation + commit atomically</name>
  <files>pyproject.toml, .github/workflows/test.yml, mkdocs.yml, CONTRIBUTING.md, CLAUDE.md</files>

  <read_first>
    - pyproject.toml — read the FULL file before any edits; locate: line 35 (mypy_path), line 63 (version_toml), lines 72-77 (build_command). Confirm current build_command content matches the D-02 finding (trailing "uv build --package godoo" line) before removing it.
    - .github/workflows/test.yml — read in full; locate line 24 (mypy invocation with packages/godoo/src)
    - mkdocs.yml — read in full; locate line 48 (paths: [...packages/godoo/src...])
    - CONTRIBUTING.md — read in full; locate lines 39, 68, 75, 76 (packages/godoo/src references)
    - CLAUDE.md — read in full; locate lines 12, 28, 92, 110, 115 (various packages/godoo references)
    - .planning/phases/05-directory-rename/05-RESEARCH.md — the verified reference hit list table is the authoritative change list; follow it exactly
  </read_first>

  <action>
    Apply the following changes exactly as specified. Read each file fully before editing. Do not use fenced code block content as a source — read the live file.

    pyproject.toml — three edits:
      1. Line 35: change "packages/godoo/src" to "packages/godoo-client/src" (inside [tool.mypy] mypy_path)
      2. Line 63: change "packages/godoo/pyproject.toml:project.version" to "packages/godoo-client/pyproject.toml:project.version" (inside [tool.semantic_release] version_toml)
      3. Lines 72-77: the build_command — read the current value; confirm it ends with a trailing "uv build --package godoo" line (per D-02); remove ONLY that trailing line. The surviving build_command must contain exactly: uv build --package godoo-client, uv build --package godoo-testcontainers, uv build --package godoo-introspection (three lines, no trailing godoo entry).

    .github/workflows/test.yml — one edit:
      Line 24: in the mypy invocation, change "packages/godoo/src" to "packages/godoo-client/src". Do not change any other argument.

    mkdocs.yml — one edit:
      Line 48: in the mkdocstrings paths list, change "packages/godoo/src" to "packages/godoo-client/src".

    CONTRIBUTING.md — three/four edits:
      Line 39: change "uv run mypy packages/godoo/src ..." path segment to "packages/godoo-client/src"
      Lines 68, 75, 76: change any prose referencing "packages/godoo/src/godoo/services/..." to "packages/godoo-client/src/godoo/services/..."

    CLAUDE.md — five edits:
      Lines 12, 28, 92, 110, 115: change every occurrence of "packages/godoo" (not followed by -client/-testcontainers/-introspection/-meta) to "packages/godoo-client"

    After all edits, run the straggler sweep to confirm zero remaining bare references:
      grep -rn "packages/godoo" . --include="*.toml" --include="*.yml" --include="*.yaml" --include="*.md" --include="*.py" | grep -v "packages/godoo-client" | grep -v "packages/godoo-testcontainers" | grep -v "packages/godoo-introspection" | grep -v "packages/godoo-meta" | grep -v ".planning/"

    The sweep must return zero lines from tracked tool/doc files. If any straggler is found, fix it before committing.

    Commit all changes atomically (rename + lock + all reference updates in one commit):
      git add packages/godoo-client packages/godoo pyproject.toml .github/workflows/test.yml mkdocs.yml CONTRIBUTING.md CLAUDE.md uv.lock
      git commit -m "chore(pkg): rename packages/godoo to packages/godoo-client; update all path refs"

    Verify blame is preserved after commit:
      git log --follow packages/godoo-client/src/godoo/client/client.py | head -20
    Must show commits predating the rename.
  </action>

  <verify>
    <automated>grep -c "packages/godoo-client/src" pyproject.toml</automated>
    Must return 2 (mypy_path + version_toml). Also run:
    grep "packages/godoo-client/src" .github/workflows/test.yml — must return 1 match.
    grep -c "packages/godoo-client/src" mkdocs.yml — must return 1.
    grep "uv build --package godoo$" pyproject.toml — must return 0 matches (stale line removed).
  </verify>

  <acceptance_criteria>
    - pyproject.toml mypy_path contains "packages/godoo-client/src" (not "packages/godoo/src")
    - pyproject.toml version_toml is "packages/godoo-client/pyproject.toml:project.version"
    - pyproject.toml build_command contains "uv build --package godoo-client" and "uv build --package godoo-testcontainers" and "uv build --package godoo-introspection" but does NOT contain "uv build --package godoo" as a standalone entry (i.e. grep for "package godoo\"" returns 0, or the pattern "package godoo\b" excluding "-client"/"-testcontainers"/"-introspection" returns 0)
    - .github/workflows/test.yml mypy invocation references "packages/godoo-client/src" (not "packages/godoo/src")
    - mkdocs.yml paths list references "packages/godoo-client/src"
    - CONTRIBUTING.md and CLAUDE.md contain no bare "packages/godoo/" references (all updated to "packages/godoo-client/")
    - Full straggler grep (above) returns zero lines for tracked tool/doc files
    - git log --follow packages/godoo-client/src/godoo/client/client.py shows history predating the rename commit
    - git commit exits 0; commit message follows conventional commits pattern
  </acceptance_criteria>

  <done>All 8 operative path references updated; straggler sweep passes clean; single atomic commit covers git mv + lock + all reference updates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan performs no network operations, handles no user input, and touches no authentication or data-flow paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | pyproject.toml build_command | accept | Pure config-path rename; no new attack surface. Risk: typo introduces wrong --package arg. Mitigation: acceptance_criteria assert exact strings post-edit. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan. Section not applicable — no install tasks present. |

This plan is a pure filesystem rename + config string update. No network, auth, input-handling, or data-flow changes. All STRIDE categories are N/A; the only residual risk is a typo in a config string, caught by the acceptance_criteria grep checks.
</threat_model>

<verification>
After Task 2 commit, run the full straggler sweep:

  grep -rn "packages/godoo" . --include="*.toml" --include="*.yml" --include="*.yaml" --include="*.md" --include="*.py" | grep -v "packages/godoo-client" | grep -v "packages/godoo-testcontainers" | grep -v "packages/godoo-introspection" | grep -v "packages/godoo-meta" | grep -v ".planning/"

Expected output: zero lines.

Also verify git history preservation:
  git log --follow packages/godoo-client/src/godoo/client/client.py | head -5
Expected: at least one commit older than the rename commit appears.
</verification>

<success_criteria>
- packages/godoo-client/ exists; packages/godoo/ does not
- git log --follow preserves blame on at least one tracked source file
- uv sync exited 0; uv.lock references packages/godoo-client (not packages/godoo)
- All 8 operative references updated (pyproject.toml x3, test.yml x1, mkdocs.yml x1, CONTRIBUTING.md x3-4, CLAUDE.md x5)
- build_command stale "uv build --package godoo" entry removed
- Straggler grep returns zero lines in tracked tool/doc files
- Single atomic commit covers all changes
</success_criteria>

<output>
Create `.planning/phases/05-directory-rename/05-01-SUMMARY.md` when done
</output>
