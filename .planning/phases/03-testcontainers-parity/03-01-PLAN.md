---
phase: "03-testcontainers-parity"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/godoo-testcontainers/src/godoo_testcontainers/py.typed
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - TESTC-08
must_haves:
  truths:
    - "py.typed marker exists in godoo-testcontainers package root"
    - "REQUIREMENTS.md TESTC-03/04/05 are struck per D-Drop-1 protocol, coverage count is 26"
    - "ROADMAP.md Phase 3 requirements line drops TESTC-03/04/05; success criteria 3 and 4 are rephrased"
    - "ROADMAP.md success criterion 1 path reads cwd/.odoo-testcontainers/snapshots/ not ~/.odoo-testcontainers/snapshots/"
    - "PROJECT.md Active block drops four-provisioner bullet and adds properties-only bullet"
  artifacts:
    - path: "packages/godoo-testcontainers/src/godoo_testcontainers/py.typed"
      provides: "PEP 561 typed-package marker for godoo-testcontainers"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Struck TESTC-03/04/05, updated traceability, coverage count 26"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 requirements and success criteria amended per D-Drop-1 + D-Snap-3-amendment"
    - path: ".planning/PROJECT.md"
      provides: "Active block + Key Decisions row amended per D-Drop-1"
  key_links:
    - from: "packages/godoo-testcontainers/src/godoo_testcontainers/py.typed"
      to: "packages/godoo-testcontainers/pyproject.toml"
      via: "hatchling wheel target includes src/godoo_testcontainers/"
      pattern: "packages.*src/godoo_testcontainers"
---

<objective>
Create the py.typed PEP 561 marker for godoo-testcontainers, and apply the D-Drop-1 / D-Snap-3-amendment charter edits that formally remove TESTC-03/04/05 from the v1 scope and correct the snapshot cache path text across all charter documents.

Purpose: Unblock mypy --strict on the testcontainers package (TESTC-08) and align all planning artifacts with the confirmed scope before implementation begins. These are mechanical, zero-risk edits that should be done first so downstream plans operate against accurate source-of-truth documents.

Output: py.typed file, amended REQUIREMENTS.md / ROADMAP.md / PROJECT.md with no stale TESTC-03/04/05 references.
</objective>

<execution_context>
@C:\Users\marc\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\marc\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-testcontainers-parity/03-CONTEXT.md
</context>

<interfaces>
<!-- Existing py.typed precedents (confirmed by PATTERNS.md): -->
<!-- packages/godoo/src/godoo/py.typed — empty file, no content -->
<!-- packages/godoo-introspection/src/godoo_introspection/py.typed — empty file, no content -->

<!-- hatchling auto-includes py.typed when packages = ["src/godoo_testcontainers"] is set -->
<!-- No pyproject.toml change needed for godoo-testcontainers (PATTERNS.md observation 6) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create py.typed PEP 561 marker for godoo-testcontainers</name>
  <files>packages/godoo-testcontainers/src/godoo_testcontainers/py.typed</files>
  <read_first>
    packages/godoo-testcontainers/src/godoo_testcontainers/__init__.py
    packages/godoo-testcontainers/pyproject.toml
  </read_first>
  <action>
    Create an empty file at packages/godoo-testcontainers/src/godoo_testcontainers/py.typed. The file must have zero content — not even a newline — identical to the precedent at packages/godoo/src/godoo/py.typed (confirmed empty by PATTERNS.md).

    Verify that packages/godoo-testcontainers/pyproject.toml already declares [tool.hatch.build.targets.wheel] packages = ["src/godoo_testcontainers"] — hatchling will then include py.typed automatically. If the wheel target declaration is absent, add it; if it already exists, do not modify pyproject.toml.

    Do NOT add a Typing :: Typed classifier to pyproject.toml unless it is already absent from the other packages too — check both packages/godoo/pyproject.toml and packages/godoo-testcontainers/pyproject.toml for consistency before deciding.
  </action>
  <verify>
    <automated>python -c "import pathlib; f=pathlib.Path('packages/godoo-testcontainers/src/godoo_testcontainers/py.typed'); assert f.exists(), 'py.typed missing'"</automated>
  </verify>
  <acceptance_criteria>
    - py.typed exists at packages/godoo-testcontainers/src/godoo_testcontainers/py.typed
    - File is empty (0 bytes)
    - packages/godoo-testcontainers/pyproject.toml has hatchling wheel target covering src/godoo_testcontainers/
    - uv run mypy --strict packages/godoo-testcontainers/src exits 0 (no new errors from the marker alone)
  </acceptance_criteria>
  <done>py.typed exists and is empty; mypy exits 0 on the package src.</done>
</task>

<task type="auto">
  <name>Task 2: Charter amendments — strike TESTC-03/04/05, fix path text, update PROJECT.md</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/phases/03-testcontainers-parity/03-CONTEXT.md
  </read_first>
  <action>
    Apply the D-Drop-1 and D-Snap-3-amendment edits to all three charter files. The required edits are enumerated in CONTEXT.md under "D-Drop-1" and "D-Snap-3-amendment". Apply every listed edit precisely — do not summarize or skip any.

    REQUIREMENTS.md edits (per D-Drop-1 + D-Snap-3-amendment):
    - Strike TESTC-03 by replacing its line with: `- ~~**TESTC-03**: A partners provisioner seeds \`res.partner\` records into the test database~~ *(dropped — D-Drop-1: declarative seeding belongs to godoo-stateman)*`
    - Strike TESTC-04 identically: `- ~~**TESTC-04**: A projects provisioner seeds \`project.project\` + \`project.task.type\` records~~ *(dropped — D-Drop-1)*`
    - Strike TESTC-05 identically: `- ~~**TESTC-05**: A users provisioner seeds \`res.users\` records with configurable groups~~ *(dropped — D-Drop-1)*`
    - Amend TESTC-01 path text from `~/.odoo-testcontainers/snapshots/` to `cwd/.odoo-testcontainers/snapshots/` (D-Snap-3-amendment).
    - Update the traceability table: mark TESTC-03, TESTC-04, TESTC-05 rows as `Dropped (D-Drop-1)` and cross them out with strikethrough markdown.
    - Update coverage count: change `29 total (INTRO-05 dropped per D-CLI-1)` to `26 total (INTRO-05, TESTC-03, TESTC-04, TESTC-05 dropped)` and adjust `Mapped to phases: 26 (100%)`.
    - Update the footer line: `*Last updated: 2026-05-22 — TESTC-03/04/05 dropped per D-Drop-1; TESTC-01 path amended per D-Snap-3-amendment*`

    ROADMAP.md edits (Phase 3 section only, per D-Drop-1 + D-Snap-3-amendment):
    - Requirements line: change `TESTC-01, TESTC-02, TESTC-03, TESTC-04, TESTC-05, TESTC-06, TESTC-07, TESTC-08` to `TESTC-01, TESTC-02, TESTC-06, TESTC-07, TESTC-08 *(TESTC-03/04/05 dropped — D-Drop-1)*`
    - Success criterion 1: change path from `~/.odoo-testcontainers/snapshots/` to `cwd/.odoo-testcontainers/snapshots/` (D-Snap-3-amendment).
    - Success criterion 3: rephrase from the four-provisioner formulation to: `harness.properties.set(...) and harness.modules.install(...) seed test state without writing raw RPC calls`
    - Success criterion 4: rephrase from "single clean API composing all four provisioners" to: `A TestHarness fixture provides a single clean API composing the snapshot cache, module install, and properties provisioner, and exposes a ready OdooClient`

    PROJECT.md edits (Active block + Key Decisions, per D-Drop-1):
    - In the Active block, under "godoo-testcontainers — parity gaps:", remove the bullet `[ ] Four resource provisioners (partners, projects, users, properties)` and replace with `[ ] Properties provisioner (ir.config_parameter k/v via ConfigParameterHelper)`.
    - Add a Key Decisions row: `| Drop TESTC-03/04/05 (partners/projects/users provisioners) from v1 | Declarative seeding belongs to godoo-stateman; testcontainers does bare minimum | D-Drop-1 |`
  </action>
  <verify>
    <automated>python -c "
import pathlib
req = pathlib.Path('.planning/REQUIREMENTS.md').read_text()
assert 'TESTC-03/04/05 dropped' in req or 'D-Drop-1' in req, 'REQUIREMENTS not amended'
assert 'cwd/.odoo-testcontainers' in req, 'path not amended in REQUIREMENTS'
road = pathlib.Path('.planning/ROADMAP.md').read_text()
assert 'cwd/.odoo-testcontainers' in road, 'path not amended in ROADMAP'
assert 'D-Drop-1' in road, 'D-Drop-1 not in ROADMAP'
proj = pathlib.Path('.planning/PROJECT.md').read_text()
assert 'D-Drop-1' in proj or 'stateman' in proj, 'PROJECT.md not amended'
print('All charter amendments verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md: TESTC-03, TESTC-04, TESTC-05 lines have strikethrough markup with D-Drop-1 rationale
    - REQUIREMENTS.md: TESTC-01 path reads cwd/.odoo-testcontainers/snapshots/ not ~/.odoo-testcontainers/snapshots/
    - REQUIREMENTS.md: Coverage count updated to 26 total (100% mapped)
    - ROADMAP.md Phase 3 Requirements line lists only TESTC-01/02/06/07/08
    - ROADMAP.md success criteria 1 path reads cwd/.odoo-testcontainers/snapshots/
    - ROADMAP.md success criteria 3 and 4 rephrased per D-Drop-1 (no mention of partners/projects/users)
    - PROJECT.md Active block has Properties provisioner bullet, not four-provisioner bullet
    - PROJECT.md Key Decisions table has D-Drop-1 row
    - No occurrence of the old string "Four resource provisioners (partners, projects, users, properties)" in PROJECT.md
  </acceptance_criteria>
  <done>All three charter files edited per D-Drop-1 and D-Snap-3-amendment; no stale references to TESTC-03/04/05 as active requirements; coverage count is 26.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File write | Charter markdown edits write to .planning/ — project-local, not network-facing |
| Package metadata | py.typed is a packaging signal to type checkers — no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | .planning/*.md charter edits | accept | Files are project-local markdown; no credential or user input involved; edits are bounded by this task's action description |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all stdlib |
</threat_model>

<verification>
After both tasks:
- `python -c "import pathlib; assert pathlib.Path('packages/godoo-testcontainers/src/godoo_testcontainers/py.typed').exists()"`
- Grep for `cwd/.odoo-testcontainers` in REQUIREMENTS.md and ROADMAP.md (must appear; `~/.odoo-testcontainers` must be gone from Phase 3 content)
- Grep for `TESTC-03/04/05` active (uncrossed) items in REQUIREMENTS.md — must find none
- `uv run mypy --strict packages/godoo-testcontainers/src` exits 0
</verification>

<success_criteria>
1. packages/godoo-testcontainers/src/godoo_testcontainers/py.typed exists and is empty
2. REQUIREMENTS.md: TESTC-03/04/05 struck, TESTC-01 path corrected, coverage count = 26
3. ROADMAP.md: Phase 3 requirements and success criteria aligned with D-Drop-1 + D-Snap-3-amendment
4. PROJECT.md: Active block and Key Decisions updated
5. mypy --strict on godoo-testcontainers/src exits 0
</success_criteria>

<output>
Create .planning/phases/03-testcontainers-parity/03-01-SUMMARY.md when done
</output>
