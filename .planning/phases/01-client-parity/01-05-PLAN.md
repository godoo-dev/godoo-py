---
phase: 01-client-parity
plan: 05
type: execute
wave: 4
gap_closure: true
depends_on: [01-04]
files_modified:
  - packages/godoo/src/godoo/client.py
  - packages/godoo/tests/test_client.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "read_binary raises a typed OdooError (OdooValidationError) — never a raw binascii.Error — when an Odoo binary field holds a malformed base64 value (CR-02)"
    - "OdooClient._safety_context has a precise static type (no `Any`) so mypy --strict checks the sentinel/None/SafetyContext states (CR review WR-03)"
    - "OdooClient.__aexit__ preserves the exception raised by the `async with` body when aclose() also fails — the close failure is logged, not propagated over the original (WR-04)"
    - "with_context / _OdooContextScope docstrings accurately describe ContextVar propagation to tasks spawned inside the block (WR-05)"
  artifacts:
    - path: "packages/godoo/src/godoo/client.py"
      provides: "Hardened read_binary, typed _safety_context sentinel, exception-safe __aexit__, accurate with_context docstrings"
      contains: "except (ValueError, TypeError)"
    - path: "packages/godoo/tests/test_client.py"
      provides: "Regression tests for the malformed-base64 path and the __aexit__ exception-preservation path"
      contains: "test_read_binary_malformed_base64"
  key_links:
    - from: "read_binary base64 decode"
      to: "OdooValidationError"
      via: "try/except (ValueError, TypeError) around base64.b64decode"
      pattern: "raise OdooValidationError"
    - from: "OdooClient._safety_context"
      to: "_UndefinedType sentinel"
      via: "typed attribute annotation SafetyContext | _UndefinedType | None"
      pattern: "_safety_context: SafetyContext"
---

<objective>
Close four code-review findings on the Phase 1 client surface (see 01-REVIEW.md):
CR-02 (read_binary unguarded base64 decode), WR-03 (_safety_context typed as `Any`),
WR-04 (__aexit__ masks the body exception), and WR-05 (with_context docstrings
overpromise block containment).

Purpose: bring the Phase 1 client to a clean `passed` verification — the phase goal
and all 12 requirements are already met (5/5 success criteria, 183 tests), these are
correctness/typing hardening fixes surfaced by code review.
Output: targeted edits to client.py plus two regression tests in test_client.py.
Not in scope: CR-01 (dismissed — the keyset short-page break is correct standard
pagination; Odoo applies record rules inside search_read before LIMIT) and the minor
warnings WR-01/WR-02/WR-06 (left tracked in 01-REVIEW.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-client-parity/01-CONTEXT.md
@.planning/phases/01-client-parity/01-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard read_binary base64 decode (CR-02)</name>
  <files>
    packages/godoo/src/godoo/client.py
    packages/godoo/tests/test_client.py
  </files>
  <read_first>
    - packages/godoo/src/godoo/client.py (read_binary method, ~lines 299-311, and the import block lines 1-12)
    - packages/godoo/src/godoo/errors.py (confirm OdooValidationError signature — already imported in client.py)
    - packages/godoo/tests/test_client.py (existing read_binary tests — match the respx mock + _extract_rpc_kwargs / response-shaping pattern used there)
  </read_first>
  <behavior>
    - Test `test_read_binary_malformed_base64`: respx-mock a read() response whose binary field holds a non-decodable base64 string (e.g. "@@@notbase64@@@" or a string of length not a multiple of 4 after junk removal); assert read_binary raises OdooValidationError (use pytest.raises). Assert the raised exception is an OdooError subclass and NOT a bare binascii.Error/ValueError.
    - Existing read_binary tests for valid base64 and the False/None unset case must still pass unchanged.
  </behavior>
  <action>
    In `read_binary` in client.py, the final line `return base64.b64decode(raw)` decodes without
    any guard. A corrupt Odoo binary field makes base64.b64decode raise binascii.Error (a
    ValueError subclass), or TypeError if `raw` is not str/bytes — both escape as non-OdooError
    exceptions, violating the typed-exception contract in CLAUDE.md.

    Replace the bare `return base64.b64decode(raw)` with a guarded decode:
    - Wrap it in try/except catching `(ValueError, TypeError)` (binascii.Error is a ValueError
      subclass, so catching ValueError covers it — do NOT add an `import binascii`).
    - In the except handler, `raise OdooValidationError(...) from exc` with a message naming the
      model, record_id, and field — e.g. f"Binary field {field!r} on {model}:{record_id} is not valid base64".
    - OdooValidationError is already imported at client.py line 12 — do not change imports.
    - Do NOT change the `raw is False or raw is None -> return b""` branch, the OdooMissingError
      branch, or the method signature/return type.

    Add the `test_read_binary_malformed_base64` test to test_client.py next to the existing
    read_binary tests, following the same respx mock style already used in that file.
  </action>
  <verify>
    <automated>uv run pytest packages/godoo/tests/test_client.py -m "not integration" -k "read_binary" -q</automated>
  </verify>
  <done>
    client.py read_binary wraps base64.b64decode in try/except (ValueError, TypeError) and raises OdooValidationError from the caught exception.
    test_client.py contains test_read_binary_malformed_base64 asserting OdooValidationError is raised for a malformed payload.
    uv run pytest packages/godoo/tests/test_client.py -m "not integration" -k "read_binary" -q exits 0.
    uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0.
    uv run ruff check . exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace _safety_context `Any` with a typed sentinel (WR-03)</name>
  <files>
    packages/godoo/src/godoo/client.py
  </files>
  <read_first>
    - packages/godoo/src/godoo/client.py (line 36 `_UNDEFINED = object()`; line 83 `self._safety_context: Any = _UNDEFINED`; set_safety_context ~line 102; _effective_safety ~lines 105-111; import block lines 1-12)
    - packages/godoo/src/godoo/safety/__init__.py (resolve_safety_context signature ~line 76 — confirm its parameter type so the call sites still type-check)
  </read_first>
  <action>
    `_safety_context` is annotated `Any` (client.py line 83), which defeats mypy --strict and
    violates the project no-`Any` rule. It holds one of three states: the _UNDEFINED sentinel,
    None (explicitly disabled), or a SafetyContext.

    Replace the bare-object sentinel with a typed one:
    - Change line 36 `_UNDEFINED = object()` to a small sentinel class plus a single instance:
      define `class _UndefinedType:` with a one-line docstring describing the "use config default"
      sentinel state, then `_UNDEFINED: Final = _UndefinedType()`.
    - Add `Final` to the existing `from typing import ...` line (currently imports
      TYPE_CHECKING, Any, cast, overload). Keep `Any` — it is still used elsewhere in the file.
    - Change the attribute annotation at line 83 from `self._safety_context: Any = _UNDEFINED`
      to `self._safety_context: SafetyContext | _UndefinedType | None = _UNDEFINED`.
    - Verify `set_safety_context(ctx: SafetyContext | None)` and `_effective_safety()` still
      type-check against the new union. The `is _UNDEFINED` / `is not _UNDEFINED` identity
      checks narrow correctly under the union — keep them as-is. Simplify only if mypy demands it;
      do not change runtime behavior.

    Do NOT change the three-state semantics, the comment block at lines 80-82, or any behavior —
    this is a typing-only change.
  </action>
  <verify>
    <automated>uv run mypy packages/godoo/src packages/godoo-testcontainers/src</automated>
  </verify>
  <done>
    client.py defines `_UndefinedType` and `_UNDEFINED: Final = _UndefinedType()`; no `_UNDEFINED = object()` remains.
    `_safety_context` is annotated `SafetyContext | _UndefinedType | None` — no `Any` on that attribute.
    uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0.
    uv run ruff check . exits 0.
    uv run pytest packages/ -m "not integration" -q exits 0 (no behavior change).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make __aexit__ preserve the body exception (WR-04)</name>
  <files>
    packages/godoo/src/godoo/client.py
    packages/godoo/tests/test_client.py
  </files>
  <read_first>
    - packages/godoo/src/godoo/client.py (__aenter__/__aexit__ ~lines 414-428; aclose ~line 412; module logger `logger = logging.getLogger("godoo.client")` at line 33)
    - packages/godoo/tests/test_client.py (existing async-context-manager test — match its style)
  </read_first>
  <behavior>
    - Test `test_aexit_preserves_body_exception_when_aclose_fails`: construct a client whose
      aclose() raises (e.g. monkeypatch/replace aclose or the transport.aclose to raise
      RuntimeError); enter `async with` and raise a distinct exception (e.g. ValueError) in the
      body; assert the exception that propagates out is the body's ValueError, NOT the aclose
      RuntimeError.
    - Existing async-context-manager test (clean enter/exit authenticates + closes) still passes.
  </behavior>
  <action>
    `__aexit__` currently just does `await self.aclose()`. If the `async with` body raised and
    aclose() also raises, the aclose exception replaces (masks) the original body exception.

    Rewrite the `__aexit__` body to:
    - `try: await self.aclose()`
    - `except Exception:` — if `exc_val is None` (clean block exit), re-raise (a close failure on
      a clean exit is a real error worth surfacing); otherwise call
      `logger.warning("aclose() failed during __aexit__; preserving the original exception", exc_info=True)`
      and let the method return normally so the original body exception propagates.
    - Keep the return type `-> None` (do not return True — never suppress the body exception).
    - Use the existing module `logger` (client.py line 33) — do not create a new logger.

    Update the `__aexit__` docstring to state that the body exception is preserved when aclose()
    fails. Do not change `__aenter__` or `aclose`.
  </action>
  <verify>
    <automated>uv run pytest packages/godoo/tests/test_client.py -m "not integration" -k "aexit or context_manager" -q</automated>
  </verify>
  <done>
    __aexit__ wraps aclose() in try/except; on failure with a non-None exc_val it logs and returns (original exception preserved); on a clean exit it re-raises.
    test_client.py contains test_aexit_preserves_body_exception_when_aclose_fails and it passes.
    uv run pytest packages/ -m "not integration" -q exits 0.
    uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0.
    uv run ruff check . exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Correct with_context / _OdooContextScope docstrings (WR-05)</name>
  <files>
    packages/godoo/src/godoo/client.py
  </files>
  <read_first>
    - packages/godoo/src/godoo/client.py (`_OdooContextScope` class ~lines 45-60; `with_context` method ~lines 205-208)
    - .planning/phases/01-client-parity/01-CONTEXT.md (D-02..D-06 — the locked with_context design; D-06 chose contextvars.ContextVar specifically for task-local safety)
  </read_first>
  <action>
    This is a docstring-accuracy fix only — NO behavior change. The current docstrings
    ("threads ambient RPC context for the duration of a with block" / "merges kwargs into every
    RPC call in its block") can be read as implying the context is strictly confined to the
    block. Because the mechanism is `contextvars.ContextVar`, a task spawned inside the block via
    `asyncio.create_task()` copies the current context at creation time and therefore inherits
    the ambient context — and keeps it even after the block exits, since `__exit__` only resets
    the variable in the task that entered the block.

    Update both docstrings to describe this accurately:
    - `_OdooContextScope` class docstring and `with_context` method docstring should state that
      the ambient context applies to all RPC calls in the current task within the block, AND that
      tasks created inside the block (e.g. via asyncio.create_task) inherit a copy of the ambient
      context per standard ContextVar semantics — that copy is independent and is not reset when
      the block exits.
    - Keep it concise (1-3 sentences each). Do not change `__enter__`, `__exit__`, the
      ContextVar, `with_context`'s signature/return, or any logic.
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run mypy packages/godoo/src packages/godoo-testcontainers/src</automated>
  </verify>
  <done>
    _OdooContextScope and with_context docstrings document ContextVar propagation to tasks spawned inside the block.
    No code/logic changed in this task (docstrings only).
    uv run ruff check . exits 0.
    uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0.
    uv run pytest packages/ -m "not integration" -q exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Odoo binary field value → read_binary base64 decode | Untrusted/possibly-corrupt server data decoded into bytes |
| `async with OdooClient` body exception → __aexit__ | Exception flow during resource teardown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | read_binary on malformed input | mitigate | Guard base64 decode; raise typed OdooValidationError instead of leaking binascii.Error — callers can catch OdooError uniformly (Task 1). |
| T-05-02 | Tampering | __aexit__ error handling | mitigate | Preserve the original body exception when aclose() fails so genuine errors are not masked by teardown noise (Task 3). |
| T-05-03 | Information Disclosure | _safety_context typing | accept | Typing-only change; no runtime/behavior impact (Task 2). |
</threat_model>

<verification>
- uv run pytest packages/ -m "not integration" -q exits 0 (all tests incl. 2 new regression tests)
- uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0
- uv run ruff check . exits 0
- uv run ruff format --check . exits 0
- grep "except (ValueError, TypeError)" packages/godoo/src/godoo/client.py finds the read_binary guard
- grep "_UndefinedType" packages/godoo/src/godoo/client.py finds the typed sentinel
</verification>

<success_criteria>
- read_binary raises OdooValidationError (a typed OdooError) on malformed base64 — covered by a regression test
- _safety_context is precisely typed; no `Any` on that attribute; mypy --strict clean
- __aexit__ preserves the body exception when aclose() fails — covered by a regression test
- with_context / _OdooContextScope docstrings accurately describe ContextVar task propagation
- All 183 pre-existing tests still pass; CR-01 dismissed; WR-01/02/06 left tracked in 01-REVIEW.md
</success_criteria>

<output>
Create `.planning/phases/01-client-parity/01-05-SUMMARY.md` when done
</output>
