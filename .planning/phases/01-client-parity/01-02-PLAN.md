---
phase: 01-client-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/godoo/src/godoo/services/cdc/service.py
  - packages/godoo/src/godoo/py.typed
autonomous: true
requirements:
  - FIXES-01
  - CLIENT-10

must_haves:
  truths:
    - "CdcService.get_feed returns a directly usable async iterator — callers can do async for event in client.cdc.get_feed(opts) without await"
    - "The godoo package ships a py.typed marker, making it a PEP 561 typed package"
  artifacts:
    - path: "packages/godoo/src/godoo/services/cdc/service.py"
      provides: "Fixed CdcService.get_feed as plain def"
      contains: "def get_feed"
    - path: "packages/godoo/src/godoo/py.typed"
      provides: "PEP 561 marker file"
  key_links:
    - from: "CdcService.get_feed (plain def)"
      to: "get_feed function in functions.py (async generator)"
      via: "direct return — no coroutine wrapper"
      pattern: "def get_feed.*-> AsyncIterator"
---

<objective>
Two mechanical fixes: (1) fix CdcService.get_feed so it is a plain def returning the async generator directly (FIXES-01), and (2) create the empty py.typed marker file for PEP 561 compliance (CLIENT-10).

Purpose: FIXES-01 makes CdcService.get_feed actually usable from the class API. CLIENT-10 enables downstream type checkers to use godoo's inline type annotations.
Output: One-word fix to service.py, one empty file py.typed.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-client-parity/01-CONTEXT.md
@.planning/phases/01-client-parity/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CdcService.get_feed class-API bug (FIXES-01)</name>
  <files>
    packages/godoo/src/godoo/services/cdc/service.py
  </files>
  <read_first>
    - packages/godoo/src/godoo/services/cdc/service.py (full file — 39 lines)
    - packages/godoo/src/godoo/services/cdc/functions.py (lines 128-135 — confirm get_feed is an async generator function)
    - .planning/codebase/CONCERNS.md lines 53-57 (bug description and fix)
    - .planning/phases/01-client-parity/01-PATTERNS.md lines 383-399 (exact before/after)
  </read_first>
  <behavior>
    - Test: Calling client.cdc.get_feed(opts) returns an object that is an AsyncIterator (not a coroutine)
    - Test: isinstance(client.cdc.get_feed(opts), AsyncGenerator) is True (or hasattr(..., "__aiter__"))
    - Test: The method is a plain function (inspect.iscoroutinefunction(CdcService.get_feed) is False)
  </behavior>
  <action>
    In packages/godoo/src/godoo/services/cdc/service.py, change line 38 from:
    `async def get_feed(self, options: GetFeedOptions) -> AsyncIterator[TrackingEvent]:`
    to:
    `def get_feed(self, options: GetFeedOptions) -> AsyncIterator[TrackingEvent]:`

    That is the complete change — remove the `async` keyword. The body `return get_feed(self._client, options)`
    is correct as-is. No other changes to this file.

    The TYPE_CHECKING imports and the AsyncIterator annotation on the return type are correct and must
    remain unchanged.

    Do NOT modify functions.py — the bug is only in service.py.
    Do NOT add an `await` anywhere — get_feed in functions.py is an async generator, not a coroutine.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from godoo.services.cdc.service import CdcService; print('PASS' if not inspect.iscoroutinefunction(CdcService.get_feed) else 'FAIL')"</automated>
  </verify>
  <done>
    CdcService.get_feed is a plain def (not async def).
    inspect.iscoroutinefunction(CdcService.get_feed) returns False.
    uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0.
    uv run ruff check . exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create py.typed PEP 561 marker (CLIENT-10)</name>
  <files>
    packages/godoo/src/godoo/py.typed
  </files>
  <read_first>
    - packages/godoo/pyproject.toml (confirm packages = ["src/godoo"] — hatchling includes all files automatically)
    - .planning/phases/01-client-parity/01-PATTERNS.md lines 403-407
  </read_first>
  <action>
    Create an empty file at packages/godoo/src/godoo/py.typed.
    The file must have no content — zero bytes. PEP 561 requires only its presence.

    No changes to pyproject.toml are required — hatchling's `packages = ["src/godoo"]` configuration
    automatically includes all files under src/godoo/ in the wheel, including non-.py files.

    Do NOT add any text, comments, or content to py.typed.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; p = pathlib.Path('packages/godoo/src/godoo/py.typed'); print('PASS' if p.exists() else 'FAIL')"</automated>
  </verify>
  <done>
    File packages/godoo/src/godoo/py.typed exists (size 0 bytes).
    uv run python -c "import godoo; import pathlib; print('PASS')" exits 0 (package still importable).
    uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0.
    uv run ruff check . exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CdcService.get_feed → async generator in functions.py | The fix changes how the generator is returned to callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CdcService.get_feed fix | accept | Removing `async` is a one-word fix with minimal attack surface. The underlying get_feed generator in functions.py is unchanged. |
| T-02-02 | Information Disclosure | py.typed marker | accept | Empty file; no information disclosure risk. PEP 561 compliance is a packaging convention. |
</threat_model>

<verification>
- uv run python -c "import inspect; from godoo.services.cdc.service import CdcService; assert not inspect.iscoroutinefunction(CdcService.get_feed)" exits 0
- python -c "import pathlib; assert pathlib.Path('packages/godoo/src/godoo/py.typed').exists()" exits 0
- uv run pytest packages/ -m "not integration" -q exits 0 (no regressions)
- uv run mypy packages/godoo/src packages/godoo-testcontainers/src exits 0
- uv run ruff check . exits 0
</verification>

<success_criteria>
- CdcService.get_feed is `def` (not `async def`) — verifiable with inspect.iscoroutinefunction
- packages/godoo/src/godoo/py.typed exists and is empty
- All existing tests still pass
- mypy --strict passes on all src/ directories
</success_criteria>

<output>
Create `.planning/phases/01-client-parity/01-02-SUMMARY.md` when done
</output>
