---
phase: 11-codegen-metadata-typed-writes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/godoo-introspection/src/godoo/introspection/type_mapper.py
  - packages/godoo-introspection/src/godoo/introspection/codegen.py
  - packages/godoo-introspection/tests/test_type_mapper.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - GEN-01

must_haves:
  truths:
    - "pydantic_field_str() returns a 4-tuple (annotation, default, imports, extra_dict) where extra_dict carries odoo_readonly/odoo_x2many metadata"
    - "D-04: readonly exclusion uses the narrower rule readonly=True OR (store=False AND compute is not None); plain non-stored inverse fields (store=False, compute=None) are writable and must NOT be marked readonly; ROADMAP Phase 11 SC-4 wording is updated to match"
    - "one2many and many2many fields carry json_schema_extra={'odoo_x2many': True} in generated output"
    - "Plain writable scalar fields return extra_dict = {}"
    - "x2many fields with extra emit Field(default_factory=list, json_schema_extra=...) — no mutable default"
    - "Generated modules gain 'from pydantic import Field' only when at least one field carries metadata"
    - "All existing tests pass after 3-tuple → 4-tuple unpack update"
  artifacts:
    - path: "packages/godoo-introspection/src/godoo/introspection/type_mapper.py"
      provides: "4-tuple pydantic_field_str() with metadata emission (D-03 + D-02)"
    - path: "packages/godoo-introspection/src/godoo/introspection/codegen.py"
      provides: "Field() emission, need_field_import flag, from pydantic import Field header"
    - path: "packages/godoo-introspection/tests/test_type_mapper.py"
      provides: "4-tuple unpack in all existing tests + 8 new metadata-assertion tests"
  key_links:
    - from: "type_mapper.py"
      to: "codegen.py"
      via: "pydantic_field_str() return value 4th element"
      pattern: "annotation, default, imports, extra = pydantic_field_str"
    - from: "codegen.py"
      to: "generated module"
      via: "need_field_import flag → header line"
      pattern: "from pydantic import Field"
---

<objective>
Teach the introspection codegen to stamp per-field write-metadata (GEN-01) so the Wave 2 write serializer can determine — without any schema lookups — which fields are read-only, computed, or x2many.

Purpose: GEN-01 is the prerequisite for WRITE-04 (readonly exclusion) and WRITE-05 (x2many guard). Both serializer behaviours depend on `json_schema_extra` present on the generated `FieldInfo`.
Output: Updated `type_mapper.py` (4-tuple return), updated `codegen.py` (conditional `Field()` emit + `from pydantic import Field` header), updated `test_type_mapper.py` (all unpacks widened + 8 new assertion tests). ROADMAP.md SC-4 wording aligned with D-04.
</objective>

<execution_context>
@/home/marc/.claude/get-shit-done/workflows/execute-plan.md
@/home/marc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-codegen-metadata-typed-writes/11-CONTEXT.md
@.planning/phases/11-codegen-metadata-typed-writes/11-RESEARCH.md
@.planning/phases/11-codegen-metadata-typed-writes/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen pydantic_field_str() to 4-tuple and update test unpacks</name>
  <read_first>
    packages/godoo-introspection/src/godoo/introspection/type_mapper.py
    packages/godoo-introspection/src/godoo/introspection/types.py
    packages/godoo-introspection/tests/test_type_mapper.py
  </read_first>
  <files>
    packages/godoo-introspection/src/godoo/introspection/type_mapper.py
    packages/godoo-introspection/tests/test_type_mapper.py
  </files>
  <behavior>
    - pydantic_field_str() signature changes: return type becomes tuple[str, str, frozenset[str], dict[str, bool]]
    - Compute extra_dict once before ttype dispatch: if field.readonly or (not field.store and field.compute is not None) → extra["odoo_readonly"] = True; if field.ttype in ("one2many", "many2many") → extra["odoo_x2many"] = True
    - Every existing return statement appends extra as 4th element — no return may remain a 3-tuple
    - All ~20 existing test unpacks change from (ann, default, imports) to (ann, default, imports, extra)
    - test_readonly_field_emits_odoo_readonly_metadata: _field_ro("char") → extra == {"odoo_readonly": True}
    - test_computed_nonstored_emits_odoo_readonly_metadata: store=False, compute="_compute_f" → extra == {"odoo_readonly": True}
    - test_nonstored_without_compute_no_metadata: store=False, compute=None → "odoo_readonly" not in extra (D-04 refinement)
    - test_one2many_emits_odoo_x2many_metadata: ttype="one2many" → extra.get("odoo_x2many") is True
    - test_many2many_emits_odoo_x2many_metadata: ttype="many2many" → extra.get("odoo_x2many") is True
    - test_plain_writable_field_no_metadata: ttype="char" (writable) → extra == {}
    - test_readonly_x2many_emits_both_flags: ttype="one2many", readonly=True → both odoo_readonly and odoo_x2many True
    - test_boolean_readonly_emits_odoo_readonly: ttype="boolean", readonly=True → extra == {"odoo_readonly": True}
  </behavior>
  <action>
    In type_mapper.py, change the return type annotation to tuple[str, str, frozenset[str], dict[str, bool]]. At the top of pydantic_field_str() (before the ttype dispatch chain), compute extra: dict[str, bool] = {} with the two rules (D-03: readonly or non-stored-with-compute → odoo_readonly; D-02: ttype in one2many/many2many → odoo_x2many). Append extra as the 4th element to every return statement. Update the docstring to document the 4th element. In test_type_mapper.py, add helper builders _field_ro() and _field_computed_nonstored(), then add the 8 new test functions from 11-PATTERNS.md. Change all existing test unpack lines from 3 to 4 vars — the existing assertions on (ann, default, imports) remain unchanged, but a 4th extra variable is unpacked and ignored in tests that do not check metadata. Note: FieldSchema already has readonly: bool = False, store: bool = True, compute: str | None = None — no FieldSchema changes needed.
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-introspection/tests/test_type_mapper.py -x -q</automated>
  </verify>
  <done>All existing tests pass with 4-tuple unpack. All 8 new metadata tests pass. mypy --strict exits 0 on packages/godoo-introspection/src.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update codegen.py to emit Field() with json_schema_extra and conditional pydantic import</name>
  <read_first>
    packages/godoo-introspection/src/godoo/introspection/codegen.py
    packages/godoo-introspection/tests/test_codegen.py
  </read_first>
  <files>
    packages/godoo-introspection/src/godoo/introspection/codegen.py
    packages/godoo-introspection/tests/test_codegen.py
  </files>
  <behavior>
    - Line 139 unpack widens: annotation, default, imports, extra = pydantic_field_str(...)
    - need_field_import: bool = False initialized alongside the other bool flags (lines 111-116)
    - After unpack: if extra is non-empty — if default == "[]": default_expr = f"Field(default_factory=list, json_schema_extra={extra!r})" else: default_expr = f"Field(default={default}, json_schema_extra={extra!r})"; need_field_import = True; else: default_expr = default
    - field_lines.append uses default_expr instead of bare default
    - Header assembly: after the existing "if need_ref: lines.append(...)" block, add "if need_field_import: lines.append('from pydantic import Field')"
    - test_codegen.py: add test_readonly_field_emits_field_with_json_schema_extra asserting the generated source contains 'json_schema_extra={"odoo_readonly": True}' for a readonly field; add test_x2many_field_emits_default_factory asserting 'Field(default_factory=list' appears and mutable default '= []' does not appear when x2many; add test_field_import_added_when_metadata_present asserting 'from pydantic import Field' appears; add test_no_field_import_when_no_metadata asserting the import is absent for all-plain-scalar models
  </behavior>
  <action>
    In codegen.py, find the existing line 139 (annotation, default, imports = pydantic_field_str(...)) and update the unpack to 4 vars. Add need_field_import = False alongside the existing bool flags at lines 111-116. Replace the field_lines.append line with the conditional Field()/plain emit block as described in 11-PATTERNS.md. In the header assembly section, after the need_ref conditional block (line 191-192), add the need_field_import conditional. In test_codegen.py, add the 4 new test functions listed above; each calls gen.generate() on a crafted ModelSchema and asserts on the resulting string. Keep all existing tests passing — this is purely additive to codegen output. Caution: when extra is non-empty and default == "[]", emit Field(default_factory=list, ...) not Field(default=[], ...) — mutable default raises PydanticUserError at model import time (Pitfall 3 in RESEARCH.md).
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-introspection/tests/ -x -q</automated>
  </verify>
  <done>All introspection tests pass. Generated source for a readonly field contains Field(default=None, json_schema_extra={'odoo_readonly': True}). Generated source for an x2many field contains Field(default_factory=list, json_schema_extra={'odoo_x2many': True}). Generated source with metadata fields contains 'from pydantic import Field'. Generated source with no metadata fields does not contain 'from pydantic import Field'. mypy --strict exits 0 on packages/godoo-introspection/src.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md SC-4 wording to match D-04 refinement</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/11-codegen-metadata-typed-writes/11-CONTEXT.md
  </read_first>
  <files>
    .planning/ROADMAP.md
  </files>
  <action>
    In ROADMAP.md, find the Phase 11 success criterion SC-4 which currently reads approximately: "Generated model fields include json_schema_extra={'odoo_readonly': True} where readonly=True or store=False". Update it to match D-04: the rule is "readonly=True OR (store=False AND compute is not None)". The new wording must clearly state both conditions and the AND requirement: a field is marked odoo_readonly when its Odoo schema has readonly=True OR (store=False AND compute is not None). Plain non-stored inverse fields (store=False, compute=None) are writable and must NOT be marked readonly. This keeps the ROADMAP spec consistent with the implementation so the plan checker and verifier agree (Pitfall 5 in RESEARCH.md).
  </action>
  <verify>
    <automated>grep -n "store=False AND compute" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md Phase 11 SC-4 contains the narrowed rule: readonly=True OR (store=False AND compute is not None). The previous broad "store=False" wording is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codegen → generated .py module | Codegen writes Python source to disk; field names sourced from Odoo schema |
| FieldSchema metadata → json_schema_extra | readonly/store/compute values from live introspection RPC become embedded in generated class definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | codegen field-name emission | accept | Field names are already validated as Python identifiers and filtered for keywords/Pydantic reserved names (existing codegen guard at lines 124-136); this plan adds no new field-name surfaces |
| T-11-01-02 | Information Disclosure | json_schema_extra in generated source | accept | Metadata values are boolean flags (odoo_readonly, odoo_x2many) — no sensitive schema data (field labels, selection values) leaks beyond what codegen already emits; severity: low |
| T-11-01-03 | Tampering | pydantic_field_str 4-tuple contract breakage | mitigate | Type annotation on return tuple enforced; all callers (codegen.py + test_type_mapper.py) updated in this plan; mypy --strict catches any missed unpack |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run full introspection test suite + mypy:

```
uv run pytest packages/godoo-introspection/tests/ -x -q
uv run ruff check packages/godoo-introspection/src packages/godoo-introspection/tests
uv run mypy packages/godoo-introspection/src
```

All three exit 0.

Spot-check generated output by calling generate() on a crafted ModelSchema with a readonly float field and an o2m field — verify the resulting string contains Field(default=None, json_schema_extra={'odoo_readonly': True}) and Field(default_factory=list, json_schema_extra={'odoo_x2many': True}) respectively.

Verify ROADMAP.md SC-4 wording contains "compute is not None".
</verification>

<success_criteria>
1. pydantic_field_str() returns a 4-tuple with correct metadata for all ttype/flag combinations.
2. Codegen emits Field() with json_schema_extra only for fields that carry metadata.
3. Codegen adds "from pydantic import Field" to the header only when needed.
4. x2many fields with metadata emit Field(default_factory=list, ...) — not Field(default=[], ...).
5. All existing + new tests in test_type_mapper.py and test_codegen.py pass.
6. mypy --strict exits 0 on packages/godoo-introspection/src.
7. ROADMAP.md SC-4 wording matches D-04: readonly=True OR (store=False AND compute is not None).
</success_criteria>

<output>
Create `.planning/phases/11-codegen-metadata-typed-writes/11-01-SUMMARY.md` when done.
</output>
