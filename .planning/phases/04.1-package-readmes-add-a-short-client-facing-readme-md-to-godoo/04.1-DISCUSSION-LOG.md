# Phase 04.1: Package READMEs - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-22
**Phase:** 04.1-package-readmes
**Areas discussed:** 0.2.1 release trigger, README content & shape, Cross-linking & meta README, godoo-ts parity

---

## 0.2.1 Release Trigger

| Option | Description | Selected |
|--------|-------------|----------|
| fix: commit | Treat empty PyPI pages as a packaging bug. Commit READMEs + readme keys as fix(...); semantic-release auto-cuts 0.2.1 on merge to main. No config change. | ✓ |
| Force patch in CI | Keep a docs: commit, trigger release.yml via workflow_dispatch with semantic-release --force patch. Manual CI step. | |
| Add docs to patch_tags | Change patch_tags to include 'docs'. Future docs commits would also bump patch — broader side effect. | |

**User's choice:** fix: commit
**Notes:** semantic-release config confirmed as `patch_tags = ["fix", "perf"]`, `minor_tags = ["feat"]`, so a `docs:` commit would not bump the version. All four pyprojects bump in lockstep via `version_toml` — meta also goes to 0.2.1 (harmless).

---

## README Content & Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal + tiny usage | Title, one-liner (from pyproject description), pip install, short runnable usage snippet, link to docs/family. | ✓ |
| Bare minimum | Title + one-liner + install + import line + license, mirroring meta README blocks; no usage example. | |
| Fuller quickstart | Minimal plus a multi-step quickstart per package. Risks duplicating the docs site. | |

**User's choice:** Minimal + tiny usage
**Notes:** Usage snippet should show each package's primary entry point (OdooClient / Introspector / OdooTestContainer).

---

## Cross-linking & Meta README

| Option | Description | Selected |
|--------|-------------|----------|
| Link family + docs, meta unchanged | Each README links to family (meta/repo) and docs site; meta README left as-is. | ✓ |
| Link family only, meta unchanged | Link family/repo only, no docs link; meta untouched. | |
| Also refresh meta README | Update the meta README too. Expands scope. | |

**User's choice:** Link family + docs, meta unchanged
**Notes:** Meta README already lists all packages and renders correctly.

---

## godoo-ts Parity

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror TS shape, Python idioms | Match godoo-ts package README structure/tone; Python install/import/usage. | |
| Write fresh, no TS reference | Author from meta README template + pyproject descriptions only; do not consult godoo-ts. | ✓ |

**User's choice:** Write fresh, no TS reference
**Notes:** Faster; READMEs authored independently from the meta README template. godoo-ts is NOT a canonical ref for this phase.

---

## Claude's Discretion

- Exact one-liner wording and precise usage snippet per package (within the agreed shape).
- Exact docs-site and family/repo URLs (use canonical project URLs from meta/repo metadata).
- Section ordering and heading style, provided all agreed elements are present.

## Deferred Ideas

- Refreshing/expanding the godoo-meta family README — kept out of scope.
- Adding `docs` to patch_tags — rejected (broader side effect); one-off `fix:` commit used.
- Fuller per-package quickstarts — rejected to avoid duplicating the docs site.
