---
phase: "04.1"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/godoo/pyproject.toml
  - packages/godoo-introspection/pyproject.toml
  - packages/godoo-testcontainers/pyproject.toml
  - packages/godoo/README.md
  - packages/godoo-introspection/README.md
  - packages/godoo-testcontainers/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "PyPI project page for godoo-client renders a description (not blank)"
    - "PyPI project page for godoo-introspection renders a description with a pre-alpha notice"
    - "PyPI project page for godoo-testcontainers renders a description"
    - "Each README contains: package title, one-liner, pip install block, usage snippet, links to docs and repo"
    - "hatchling can build all three packages without error (README file found and embedded)"
    - "A fix: conventional commit on develop, when merged to main, triggers the automated 0.2.1 patch release"
  artifacts:
    - path: "packages/godoo/README.md"
      provides: "godoo-client PyPI long description"
      contains: "godoo-client"
    - path: "packages/godoo-introspection/README.md"
      provides: "godoo-introspection PyPI long description with pre-alpha notice"
      contains: "Pre-Alpha"
    - path: "packages/godoo-testcontainers/README.md"
      provides: "godoo-testcontainers PyPI long description"
      contains: "godoo-testcontainers"
    - path: "packages/godoo/pyproject.toml"
      provides: "readme key wired for godoo-client"
      contains: 'readme = "README.md"'
    - path: "packages/godoo-introspection/pyproject.toml"
      provides: "readme key wired for godoo-introspection"
      contains: 'readme = "README.md"'
    - path: "packages/godoo-testcontainers/pyproject.toml"
      provides: "readme key wired for godoo-testcontainers"
      contains: 'readme = "README.md"'
  key_links:
    - from: "packages/godoo/pyproject.toml"
      to: "packages/godoo/README.md"
      via: "readme = \"README.md\" key resolved relative to pyproject.toml directory"
      pattern: 'readme = "README.md"'
    - from: "packages/godoo-introspection/pyproject.toml"
      to: "packages/godoo-introspection/README.md"
      via: "readme = \"README.md\" key resolved relative to pyproject.toml directory"
      pattern: 'readme = "README.md"'
    - from: "packages/godoo-testcontainers/pyproject.toml"
      to: "packages/godoo-testcontainers/README.md"
      via: "readme = \"README.md\" key resolved relative to pyproject.toml directory"
      pattern: 'readme = "README.md"'
---

<objective>
Fix the empty PyPI project pages for the three real godoo distributions by wiring the missing `readme = "README.md"` key into each package's `pyproject.toml` and authoring a short, client-facing README in each package root. Commit as a `fix:` conventional commit so the existing automated pipeline cuts version 0.2.1 on merge to main.

Purpose: After Phase 04 published v0.2.0, the three real package pages (godoo-client, godoo-introspection, godoo-testcontainers) show empty descriptions on PyPI because their `pyproject.toml` files lack the `readme` key hatchling needs to embed the long description in wheel metadata. Only `godoo-meta` shipped a README and renders correctly. This phase is a pure packaging defect fix.

Output: Three new `README.md` files (one per package root) + three `pyproject.toml` edits adding `readme = "README.md"` under `[project]`, committed as `fix(packaging): wire package READMEs so PyPI pages render`.
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/marc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Verified facts extracted from source inspection during planning. Use these directly. -->

<!-- Canonical PyPI names and descriptions (from respective pyproject.toml): -->
<!-- godoo-client:         name="godoo-client",         description="Async Python client for Odoo JSON-RPC" -->
<!-- godoo-introspection:  name="godoo-introspection",  description="Schema discovery and codegen for Odoo models" -->
<!-- godoo-testcontainers: name="godoo-testcontainers", description="Docker-based Odoo instances for integration testing" -->

<!-- Canonical URLs (verified from all three pyproject.toml [project.urls]): -->
<!-- Documentation: https://www.marcfargas.com/~odoopy/ -->
<!-- Repository:    https://github.com/godoo-dev/godoo-py -->

<!-- godoo-introspection classifier: "Development Status :: 2 - Pre-Alpha" (verified) -->

<!-- Working reference for readme key placement: packages/godoo-meta/pyproject.toml -->
<!-- The readme path in [project] is relative to the pyproject.toml that declares it. -->
<!-- readme = "README.md" must appear under [project], not [build-system] or anywhere else. -->

<!-- Verified primary entry points and import paths: -->
<!-- godoo-client:         from godoo.client import OdooClient, create_client -->
<!--                       create_client() is the idiomatic async factory (reads env, authenticates) -->
<!-- godoo-introspection:  from godoo.introspection import Introspector -->
<!--                       Introspector(client) — takes OdooClient as sole arg -->
<!-- godoo-testcontainers: from godoo.testcontainers import OdooTestContainer -->
<!--                       OdooTestContainer(modules=[...]).start() — start() is async -->

<!-- semantic-release patch_tags = ["fix", "perf"] — a fix: commit on main bumps to 0.2.1 -->
<!-- A docs: commit would NOT trigger a version bump (docs not in patch_tags). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire readme key in three pyproject.toml files</name>
  <files>
    packages/godoo/pyproject.toml,
    packages/godoo-introspection/pyproject.toml,
    packages/godoo-testcontainers/pyproject.toml
  </files>
  <read_first>
    packages/godoo-meta/pyproject.toml (working reference — confirm exact placement of readme key under [project]),
    packages/godoo/pyproject.toml,
    packages/godoo-introspection/pyproject.toml,
    packages/godoo-testcontainers/pyproject.toml
  </read_first>
  <action>
    Add `readme = "README.md"` under the `[project]` table in each of the three pyproject files. Placement: immediately after the `description` line in each file (for readability; order within [project] is not significant to hatchling). Do NOT touch any other key or section. Do NOT touch packages/godoo-meta/pyproject.toml (per D-06).

    The three edits are identical in shape — only the file path differs:
    - packages/godoo/pyproject.toml: add `readme = "README.md"` after `description = "Async Python client for Odoo JSON-RPC"`
    - packages/godoo-introspection/pyproject.toml: add `readme = "README.md"` after `description = "Schema discovery and codegen for Odoo models"`
    - packages/godoo-testcontainers/pyproject.toml: add `readme = "README.md"` after `description = "Docker-based Odoo instances for integration testing"`

    Do not stage or commit yet — both tasks must be complete before the final commit.
  </action>
  <verify>
    <automated>
      PowerShell: (Select-String -Path packages/godoo/pyproject.toml -Pattern 'readme = "README.md"').Count -gt 0
      PowerShell: (Select-String -Path packages/godoo-introspection/pyproject.toml -Pattern 'readme = "README.md"').Count -gt 0
      PowerShell: (Select-String -Path packages/godoo-testcontainers/pyproject.toml -Pattern 'readme = "README.md"').Count -gt 0
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/godoo/pyproject.toml contains the line: readme = "README.md"
    - packages/godoo-introspection/pyproject.toml contains the line: readme = "README.md"
    - packages/godoo-testcontainers/pyproject.toml contains the line: readme = "README.md"
    - packages/godoo-meta/pyproject.toml is unchanged (no diff)
    - No other lines in any pyproject.toml are modified
  </acceptance_criteria>
  <done>All three pyproject.toml files have the readme key under [project]; godoo-meta is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Author three package READMEs and make the fix: commit</name>
  <files>
    packages/godoo/README.md,
    packages/godoo-introspection/README.md,
    packages/godoo-testcontainers/README.md
  </files>
  <read_first>
    packages/godoo-meta/README.md (per-package block shape — the template),
    packages/godoo/pyproject.toml (description, [project.urls]),
    packages/godoo-introspection/pyproject.toml (description, classifier, [project.urls]),
    packages/godoo-testcontainers/pyproject.toml (description, [project.urls])
  </read_first>
  <action>
    Create three new README.md files, one at each package root. Each follows the D-03 shape: title + one-liner (from pyproject description) + `pip install` block + quick start snippet + links section. Do NOT create any README at the repo root. The files are siblings of their respective pyproject.toml files.

    ---- packages/godoo/README.md ----
    Title: `# godoo-client`
    One-liner: the description from packages/godoo/pyproject.toml — "Async Python client for Odoo JSON-RPC"
    Install section heading: `## Install`, fenced bash block: `pip install godoo-client`
    Quick start section heading: `## Quick start`, fenced python block showing:
      - `import asyncio`
      - `from godoo.client import create_client`
      - `async def main():`
      - `    client = await create_client()  # reads ODOO_URL / ODOO_DB / ODOO_USER / ODOO_PASSWORD`
      - `    partners = await client.search_read("res.partner", [], fields=["name"])`
      - `    await client.aclose()`
    Links section heading: `## Links`, bullet list with:
      - `[Documentation](https://www.marcfargas.com/~odoopy/)`
      - `[GitHub repository](https://github.com/godoo-dev/godoo-py)`
      - `License: LGPL-3.0-or-later`

    ---- packages/godoo-introspection/README.md ----
    Title: `# godoo-introspection`
    One-liner: "Schema discovery and codegen for Odoo models"
    IMPORTANT — immediately after the one-liner, add a pre-alpha callout paragraph:
      "> **Pre-Alpha:** This package is in early development. The API may change without notice and not all advertised features are implemented yet."
    Install section: `## Install`, fenced bash block: `pip install godoo-introspection`
    Quick start section: `## Quick start`, fenced python block showing:
      - `import asyncio`
      - `from godoo.client import create_client`
      - `from godoo.introspection import Introspector`
      - `async def main():`
      - `    client = await create_client()`
      - `    introspector = Introspector(client)`
      - `    schema = await introspector.get_schema("res.partner")`
    Links section: `## Links`, same three bullets as godoo-client README.

    ---- packages/godoo-testcontainers/README.md ----
    Title: `# godoo-testcontainers`
    One-liner: "Docker-based Odoo instances for integration testing"
    Install section: `## Install`, fenced bash block: `pip install godoo-testcontainers`
    Quick start section: `## Quick start`, fenced python block showing:
      - `import asyncio`
      - `from godoo.testcontainers import OdooTestContainer`
      - `async def main():`
      - `    container = OdooTestContainer(modules=["sale"])`
      - `    started = await container.start()`
      - `    # started.client is an authenticated OdooClient`
      - `    await started.cleanup()`
    Links section: `## Links`, same three bullets as godoo-client README.

    After all three files are written and the pyproject edits from Task 1 are in place, verify the builds succeed (see verify block), then stage all six files and commit with the exact message:
      fix(packaging): wire package READMEs so PyPI pages render

    This is a conventional `fix:` commit (per D-01) — do NOT use `docs:`. Do NOT hand-edit any version string in any pyproject.toml; semantic-release handles the bump automatically when the commit lands on main.
  </action>
  <verify>
    <automated>
      uv build --package godoo-client 2>&amp;1 | Select-String -Pattern "error|Error" | Where-Object { $_ -notmatch "no error" }
      uv build --package godoo-introspection 2>&amp;1 | Select-String -Pattern "error|Error" | Where-Object { $_ -notmatch "no error" }
      uv build --package godoo-testcontainers 2>&amp;1 | Select-String -Pattern "error|Error" | Where-Object { $_ -notmatch "no error" }

      Grep assertions (all must return matches):
      Select-String -Path packages/godoo/README.md -Pattern "create_client"
      Select-String -Path packages/godoo/README.md -Pattern "pip install godoo-client"
      Select-String -Path packages/godoo/README.md -Pattern "marcfargas.com/~odoopy"
      Select-String -Path packages/godoo/README.md -Pattern "godoo-dev/godoo-py"
      Select-String -Path packages/godoo-introspection/README.md -Pattern "Pre-Alpha"
      Select-String -Path packages/godoo-introspection/README.md -Pattern "Introspector"
      Select-String -Path packages/godoo-introspection/README.md -Pattern "pip install godoo-introspection"
      Select-String -Path packages/godoo-testcontainers/README.md -Pattern "OdooTestContainer"
      Select-String -Path packages/godoo-testcontainers/README.md -Pattern "pip install godoo-testcontainers"

      After commit: git log --oneline -1 | Select-String -Pattern "fix\(packaging\)"
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/godoo/README.md exists and contains: "godoo-client", "create_client", "pip install godoo-client", "https://www.marcfargas.com/~odoopy/", "https://github.com/godoo-dev/godoo-py", "LGPL-3.0"
    - packages/godoo-introspection/README.md exists and contains: "godoo-introspection", "Pre-Alpha", "Introspector", "pip install godoo-introspection", docs URL, repo URL
    - packages/godoo-testcontainers/README.md exists and contains: "godoo-testcontainers", "OdooTestContainer", "pip install godoo-testcontainers", docs URL, repo URL
    - uv build --package godoo-client succeeds (exit 0, no "error" in output)
    - uv build --package godoo-introspection succeeds (exit 0)
    - uv build --package godoo-testcontainers succeeds (exit 0)
    - git log --oneline -1 shows a commit matching "fix(packaging): wire package READMEs so PyPI pages render"
    - packages/godoo-meta/README.md is unchanged
    - No version string in any pyproject.toml was manually edited
  </acceptance_criteria>
  <done>Three READMEs authored, three pyproject.toml files updated, builds pass, fix: commit made on develop branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → pyproject.toml | Static TOML key addition; no user input, no runtime code path |
| Author → README.md | Static Markdown authoring; rendered by PyPI's own Markdown renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Information Disclosure | README content | accept | READMEs contain only public documentation (import paths, pip install commands, public URLs). No secrets, no credentials, no internal hostnames. All linked URLs are already public in pyproject.toml project.urls. |
| T-04.1-02 | Tampering | pyproject.toml edits | accept | Change is a single-line key addition under [project]; no build-system or dependency changes. Reviewed in code review before merge to main. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. Package Legitimacy Audit confirms no installs. Slopcheck not applicable. |

**Summary:** This phase adds static Markdown files and a single TOML key. No untrusted input enters any code path; READMEs are static content rendered by PyPI's own pipeline; no authentication, cryptography, or external service calls are introduced. No high or medium threats identified. Block-on-high passes cleanly.
</threat_model>

<verification>
Full phase verification after the fix: commit:

1. All six files are present and tracked by git:
   - packages/godoo/README.md
   - packages/godoo-introspection/README.md
   - packages/godoo-testcontainers/README.md
   - packages/godoo/pyproject.toml (contains readme = "README.md")
   - packages/godoo-introspection/pyproject.toml (contains readme = "README.md")
   - packages/godoo-testcontainers/pyproject.toml (contains readme = "README.md")

2. packages/godoo-meta/README.md and packages/godoo-meta/pyproject.toml are unchanged (git diff shows no delta for either file).

3. `uv build --package godoo-client` exits 0 and the built wheel/sdist appear in dist/.
   `uv build --package godoo-introspection` exits 0.
   `uv build --package godoo-testcontainers` exits 0.

4. `git log --oneline -1` shows: fix(packaging): wire package READMEs so PyPI pages render

5. No version string in any pyproject.toml was modified (git diff confirms only the `readme` line was added to each).

6. Introspection README contains "Pre-Alpha" (guards against Pitfall 5 from RESEARCH.md).

7. After merge to main and CI passes, semantic-release will tag v0.2.1 and publish all four distributions. Verify post-merge in GitHub Actions: Release workflow fires, tags v0.2.1, and PyPI shows the long description on all three package pages.
</verification>

<success_criteria>
Phase 04.1 is complete when:
- Three package READMEs exist at packages/{godoo,godoo-introspection,godoo-testcontainers}/README.md
- Each README contains all D-03 elements: title, one-liner, pip install, usage snippet, links (docs + repo)
- godoo-introspection README contains a pre-alpha warning so PyPI visitors are not misled
- All three pyproject.toml files have `readme = "README.md"` under [project]
- All three packages build successfully with uv build
- A fix(packaging): conventional commit is on the develop branch, ready to merge to main
- No version strings were manually edited (semantic-release handles the 0.2.0 → 0.2.1 bump on merge)
- packages/godoo-meta/README.md is unchanged
</success_criteria>

<output>
Create `.planning/phases/04.1-package-readmes-add-a-short-client-facing-readme-md-to-godoo/04.1-01-SUMMARY.md` when done.
</output>
