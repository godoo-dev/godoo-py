---
phase: "04.1"
plan: 01
subsystem: packaging
tags: [packaging, pypi, readme, fix]
dependency_graph:
  requires: []
  provides: [PyPI long descriptions for godoo-client, godoo-introspection, godoo-testcontainers]
  affects: [PyPI project pages, hatchling wheel metadata]
tech_stack:
  added: []
  patterns: [hatchling readme key, package-level README.md]
key_files:
  created:
    - packages/godoo/README.md
    - packages/godoo-introspection/README.md
    - packages/godoo-testcontainers/README.md
  modified:
    - packages/godoo/pyproject.toml
    - packages/godoo-introspection/pyproject.toml
    - packages/godoo-testcontainers/pyproject.toml
decisions:
  - "Used fix: conventional commit scope so semantic-release auto-bumps to 0.2.1 on merge to main"
  - "readme key placed immediately after description line in each [project] table for readability"
  - "Pre-Alpha callout added to godoo-introspection README per plan D-03 requirement"
metrics:
  duration: "~10 minutes"
  completed_date: "2026-05-22"
  tasks_completed: 2
  files_changed: 6
---

# Phase 04.1 Plan 01: Wire Package READMEs Summary

Wire `readme = "README.md"` into three package `pyproject.toml` files and author client-facing READMEs so godoo-client, godoo-introspection, and godoo-testcontainers render long descriptions on PyPI instead of blank pages.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Wire readme key in three pyproject.toml files | d82320b | packages/godoo/pyproject.toml, packages/godoo-introspection/pyproject.toml, packages/godoo-testcontainers/pyproject.toml |
| 2 | Author three package READMEs and make the fix: commit | d82320b | packages/godoo/README.md, packages/godoo-introspection/README.md, packages/godoo-testcontainers/README.md |

Note: Both tasks were committed together in a single `fix(packaging)` commit per plan instructions ("Do not stage or commit yet — both tasks must be complete before the final commit").

## What Was Built

Three `README.md` files (one per package root) containing: package title, one-liner description, `pip install` block, async quick-start snippet, and links to documentation and repository. The `godoo-introspection` README includes a Pre-Alpha callout paragraph. Three `pyproject.toml` files updated with `readme = "README.md"` under `[project]`. All three packages build successfully via `uv build`.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. READMEs contain real import paths (`create_client`, `Introspector`, `OdooTestContainer`) and real public URLs. No placeholder text.

## Threat Flags

None. Only static Markdown files and a single TOML key addition per package. No new network endpoints, auth paths, or trust boundaries introduced.

## Self-Check: PASSED

- packages/godoo/README.md: FOUND
- packages/godoo-introspection/README.md: FOUND
- packages/godoo-testcontainers/README.md: FOUND
- packages/godoo/pyproject.toml (readme key): VERIFIED
- packages/godoo-introspection/pyproject.toml (readme key): VERIFIED
- packages/godoo-testcontainers/pyproject.toml (readme key): VERIFIED
- Commit d82320b: FOUND (fix(packaging): wire package READMEs so PyPI pages render)
- uv build godoo-client: PASSED
- uv build godoo-introspection: PASSED
- uv build godoo-testcontainers: PASSED
- godoo-meta unchanged: VERIFIED
- No version strings hand-edited: VERIFIED (diff shows only +readme = "README.md" lines)
