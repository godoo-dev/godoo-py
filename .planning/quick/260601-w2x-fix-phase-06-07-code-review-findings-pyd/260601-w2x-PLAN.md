---
quick_id: 260601-w2x
type: quick-execute
autonomous: true
files_modified:
  - packages/godoo-client/src/godoo/client/_pydantic_transform.py
  - packages/godoo-client/src/godoo/client/client.py
  - packages/godoo-client/tests/test_pydantic_transform.py
  - packages/godoo-client/tests/test_typed_dispatch.py
  - packages/godoo-introspection/src/godoo/introspection/codegen.py
  - packages/godoo-introspection/src/godoo/introspection/type_mapper.py
  - packages/godoo-introspection/src/godoo/introspection/cli.py
  - packages/godoo-introspection/tests/test_codegen.py
  - packages/godoo-introspection/tests/test_cli.py

must_haves:
  truths:
    - "Empty x2many Odoo field (False) coerces to [] not None in the wire transform"
    - "Restricted m2o display name [id, False] coerces to Ref(id, name=None) not a raw tuple"
    - "search_read typed dispatch always injects 'id' into the fields list sent to Odoo"
    - "derive_partial_model ValueError surfaces as OdooValidationError at both dispatch sites"
    - "Pydantic reserved names (model_config etc.) and Python keywords are skipped in codegen"
    - "Class names derived from numeric-starting models are guarded against SyntaxError"
    - "Generated files are written with LF newlines on all platforms"
    - "Required imports are derived structurally from type_mapper, not by re-parsing annotation strings"
    - "CLI catches OdooError (auth/network failures) and emits clean stderr + exit code 1"
  artifacts:
    - path: packages/godoo-client/src/godoo/client/_pydantic_transform.py
      provides: "Fixed False->[] for list fields; fixed [id, False] m2o to Ref"
    - path: packages/godoo-client/src/godoo/client/client.py
      provides: "id injection in search_read typed path; ValueError->OdooValidationError wrap at both sites"
    - path: packages/godoo-client/tests/test_pydantic_transform.py
      provides: "Unit tests for findings #1 and #3"
    - path: packages/godoo-client/tests/test_typed_dispatch.py
      provides: "Unit tests for findings #2 and #7"
    - path: packages/godoo-introspection/src/godoo/introspection/codegen.py
      provides: "Reserved name guard, keyword guard, class name guard, LF writes, structural imports"
    - path: packages/godoo-introspection/src/godoo/introspection/type_mapper.py
      provides: "pydantic_field_str returns (annotation, default, imports_set)"
    - path: packages/godoo-introspection/tests/test_codegen.py
      provides: "Unit tests for findings #4, #5, #8, #9, #10"
    - path: packages/godoo-introspection/tests/test_cli.py
      provides: "Unit test for finding #6"
---

<objective>
Fix 10 verified code-review findings from the Phase 06/07 diff spanning godoo-client wire
transforms, typed dispatch, and godoo-introspection codegen/CLI. Each task is one atomic
commit group covering related findings.

Purpose: Close correctness and robustness gaps before the Phase 08 Pyodide spike begins.
Output: All 10 findings resolved with unit tests; quality gate (ruff + mypy + pytest) green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<!-- ================================================================
     TASK A — godoo-client: wire-transform fixes (#1 + #3)
     Files: _pydantic_transform.py, test_pydantic_transform.py
     ================================================================ -->
<task type="auto" tdd="true">
  <name>Task A: Fix x2many False->[] and m2o [id,False] in wire transform</name>
  <files>
    packages/godoo-client/src/godoo/client/_pydantic_transform.py
    packages/godoo-client/tests/test_pydantic_transform.py
  </files>
  <behavior>
    - Finding #1: model_validate({"id":1, "tag_ids": False}) where tag_ids: list[int] = [] produces
      tag_ids=[] (not None, not ValidationError). The list-origin check fires BEFORE the generic
      False->None coercion (use get_origin(annotation) is list to detect list-annotated fields;
      also handle list | None — get_args check).
    - Finding #3: model_validate({"id":1, "parent_id": [3, False]}) where parent_id: Ref[X] | None
      produces parent_id=Ref(id=3, name=None) (not ValidationError, not raw list). The Ref name
      field must become Optional[str] in typed.py's Ref dataclass, OR the transform sets name to
      None when value[1] is False. Because Ref is a frozen dataclass with `name: str`, the minimal
      fix is: in _annotation_mentions_ref branch, accept isinstance(value[1], (str, bool)) (False
      counts) and produce Ref(id=value[0], name=None if value[1] is False else value[1]). Update
      Ref.name type to str | None to accept None.
    - New test: test_x2many_false_becomes_empty_list — model with tag_ids: list[int] = [],
      wire value False, expect [].
    - New test: test_x2many_optional_false_becomes_empty_list — model with tag_ids: list[int] | None = None,
      wire value False, expect [] (not None — list-origin takes precedence over the optional None path).
    - New test: test_m2o_restricted_display_name — model with parent_id: Ref[object] | None = None,
      wire value [3, False], expect Ref(id=3, name=None).
    - New test in test_pydantic_transform.py's _TestPartner: add tag_ids: list[int] = [] field for the
      above tests.
  </behavior>
  <action>
    Read _pydantic_transform.py (already in context) and test_pydantic_transform.py (already in context).

    Step 1 — Write failing tests first:

    In test_pydantic_transform.py, add tag_ids: list[int] = [] to _TestPartner.
    Add three new test functions after the existing m2o tests:
      test_x2many_false_becomes_empty_list — asserts tag_ids == []
      test_x2many_optional_false_becomes_empty_list — uses a locally-defined model with
        tag_ids: list[int] | None = None, asserts wire False → []
      test_m2o_restricted_display_name — asserts Ref(id=3, name=None) for [3, False] input on parent_id

    Run tests — all three MUST fail before the fix.

    Step 2 — Fix Ref in typed.py: change name: str to name: str | None.

    Step 3 — Fix _odoo_wire_transforms in OdooBaseModel._odoo_wire_transforms:

    In the existing transform loop, BEFORE the `if value is False and not _annotation_is(...)` block,
    add a new check that handles the list-origin case:

      origin = get_origin(annotation)
      # Handle Optional[list[int]]: strip outer Optional to find the list origin
      if origin is None:
          for arg in get_args(annotation):
              if get_origin(arg) is list:
                  origin = list
                  break

      if value is False and origin is list:
          out[name] = []
          continue

    This ensures False for any list-annotated field (bare list[int] or list[int] | None) maps to []
    before the generic False->None rule fires.

    For the m2o fix, update the existing m2o branch condition:
      Change: `and isinstance(value[1], str)`
      To: `and isinstance(value[1], (str, type(False))) and isinstance(value[1], (str, bool))`
    Simpler: replace the str check with `and (isinstance(value[1], str) or value[1] is False)`.
    Then build: `Ref(id=value[0], name=None if value[1] is False else value[1])`.

    The annotation check `_annotation_mentions_ref(annotation)` stays — the [id, False] shape only
    converts for Ref-annotated fields, not for raw list fields.

    Do NOT alter bool preservation logic (D-02) or the date/datetime transforms.

    Step 4 — Run tests. All three new tests + all existing tests must pass.

    Commit message: `fix(client): x2many False→[] and m2o [id,False]→Ref in wire transform`
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-client/tests/test_pydantic_transform.py -x -q</automated>
  </verify>
  <done>
    All three new tests pass. All existing wire-transform tests still pass. No mypy errors on
    packages/godoo-client/src with --strict.
  </done>
</task>

<!-- ================================================================
     TASK B — godoo-client: typed dispatch fixes (#2 + #7)
     Files: client.py, test_typed_dispatch.py
     ================================================================ -->
<task type="auto" tdd="true">
  <name>Task B: Inject 'id' in search_read typed path; wrap ValueError as OdooValidationError</name>
  <files>
    packages/godoo-client/src/godoo/client/client.py
    packages/godoo-client/tests/test_typed_dispatch.py
  </files>
  <behavior>
    - Finding #2: search_read typed path with fields=["name"] must inject "id" into the list sent to
      Odoo so the response always contains id (Odoo search_read does NOT auto-include id unless asked).
      The injected fields list must be deduplicated ("id" + original, preserving order).
      read() typed path does NOT need this fix — Odoo read() always returns id regardless of fields list.
      Verify with a test that intercepts the outgoing RPC payload and asserts "id" appears in
      kwargs["fields"] sent to Odoo when fields=["name"] is given.
    - Finding #7: both read() and search_read() typed paths call derive_partial_model which raises
      ValueError for unknown field names. Currently only ModuleNotFoundError is caught; ValueError
      leaks unwrapped. Wrap the derive_partial_model call at both sites in a try/except ValueError
      that re-raises as OdooValidationError with the original message as cause.
    - New test: test_search_read_typed_id_injected_into_fields — mocks the RPC call, passes
      fields=["name"] to search_read(TinyPartner, ...), captures the request body via respx route
      side_effect, asserts "id" in the fields list that was sent to Odoo. The response includes "id"
      so validation succeeds.
    - New test: test_search_read_typed_id_already_present_not_duplicated — passes fields=["id","name"],
      asserts Odoo receives exactly ["id","name"] (no double id).
    - New test: test_read_typed_id_not_injected — confirms read() typed path does not inject id (it
      is already immune; test documents that no double-injection occurs when fields=["name"] is passed
      to read()).
    - New test: test_typed_dispatch_unknown_field_raises_odoo_validation — calls search_read(TinyPartner,
      fields=["nonexistent_field"]), expects OdooValidationError (not raw ValueError). Same for read().
  </behavior>
  <action>
    Read client.py and test_typed_dispatch.py (already in context).

    Step 1 — Write failing tests in test_typed_dispatch.py (4 new test functions).

    For the id-injection tests, use a respx route with a side_effect function that captures the
    parsed JSON body from the request (request.content decoded + json.loads), saves it to a
    nonlocal list, then returns the mock response. Assert that the captured kwargs["params"][0] for
    "fields" contains "id".

    Alternatively: inspect respx.calls after the call. Example pattern:
      calls = respx.calls  # list of calls; calls[0].request.content -> bytes -> json
    Use whichever approach is more readable.

    For the ValueError tests: mock RPC to return some response (it won't be reached since derive_partial_model
    raises before the RPC call), call search_read/read with a nonexistent field name. The fields
    param triggers derive_partial_model(TinyPartner, ["nonexistent_field"]) → ValueError. Assert
    OdooValidationError is raised (not ValueError).

    Step 2 — Fix client.py search_read typed dispatch (around line 297-302):
    After `if fields is not None:` in the typed branch, before calling derive_partial_model,
    build the effective fields list: inject "id" if absent:
      effective_fields = list(dict.fromkeys(["id", *fields]))
    Pass effective_fields to both derive_partial_model and to kwargs["fields"] (the RPC payload).
    Make sure the original `fields` variable (bound from the function parameter) is not mutated.

    read() typed dispatch (around line 229-234): do NOT inject id there. Add a comment:
    `# read() always returns id from Odoo regardless of fields — no injection needed.`

    Step 3 — Fix ValueError wrapping at BOTH dispatch sites:
    In read() typed dispatch, wrap `derive_partial_model(typed_model, fields)` in:
      try:
          target = derive_partial_model(typed_model, fields)
      except ValueError as exc:
          raise OdooValidationError(str(exc)) from exc
    Apply the same try/except in search_read() typed dispatch around derive_partial_model call.
    Do not catch ValueError anywhere else — only at these two call sites.

    Step 4 — Run tests. All four new tests + all existing typed-dispatch tests must pass.

    Commit message: `fix(client): inject id in search_read typed path; wrap ValueError as OdooValidationError`
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-client/tests/test_typed_dispatch.py -x -q</automated>
  </verify>
  <done>
    All four new tests pass. All existing typed-dispatch tests still pass. No mypy errors.
  </done>
</task>

<!-- ================================================================
     TASK C — godoo-introspection: codegen fixes (#4+#5+#8+#9+#10)
     Files: codegen.py, type_mapper.py, test_codegen.py
     ================================================================ -->
<task type="auto" tdd="true">
  <name>Task C: Codegen reserved-name guard, keyword guard, classname guard, LF writes, structural imports</name>
  <files>
    packages/godoo-introspection/src/godoo/introspection/codegen.py
    packages/godoo-introspection/src/godoo/introspection/type_mapper.py
    packages/godoo-introspection/tests/test_codegen.py
  </files>
  <behavior>
    Finding #4 — Reserved Pydantic names guard:
      pydantic_reserved = {"model_config","model_fields","model_validate","model_dump",
                           "model_rebuild","model_post_init","model_json_schema","schema","copy","dict","json"}
      In the field-emission loop, after the isidentifier() check, add:
        if field_name in _PYDANTIC_RESERVED_NAMES: skip with warning
      Module-level frozenset constant _PYDANTIC_RESERVED_NAMES.
      Test: test_generate_reserved_name_skipped — schema with field named "model_config" of type char;
        assert "model_config" does not appear as a field line in the generated source.

    Finding #5 — Python keyword guard:
      import keyword (stdlib). After isidentifier() check, add:
        if keyword.iskeyword(field_name): skip with warning
      Test: test_generate_keyword_field_skipped — schema with field named "class" of type char;
        assert "class:" does not appear in the generated source.

    Finding #8 — Class name validation:
      In _model_to_classname, guard the result. If not result.isidentifier(), raise ValueError
      (or return None and skip in generate/write). The least-surprising behavior: raise ValueError
      with message f"Model name {model!r} produces invalid Python identifier {result!r}".
      In CodeGenerator.generate and write, let ValueError propagate (caller / test catches it).
      Test: test_invalid_classname_raises — _model_to_classname("3d.model") result is "3DModel"
        which fails isidentifier(); assert _model_to_classname("3d.model") raises ValueError.
        Also: test_generate_invalid_classname_propagates — CodeGenerator.generate with ModelSchema
        name="3d.model" raises ValueError.

    Finding #9 — LF newlines on Windows:
      Both write_text calls must pass newline="\n":
        (output_dir / filename).write_text(source, encoding="utf-8", newline="\n")
        (output_dir / "__init__.py").write_text("\n".join(barrel_lines), encoding="utf-8", newline="\n")
      Test: test_write_newline_lf — write a schema to tmp_path, read back the file bytes (open in
        "rb" mode), assert b"\r\n" not in content (no CRLF bytes, even on Windows).
        Run this test on CI which covers Windows; it also passes on Linux (LF already default).

    Finding #10 — Structural imports from type_mapper:
      Change pydantic_field_str return type from tuple[str, str] to tuple[str, str, frozenset[str]].
      The third element is a frozenset of import tokens needed, e.g. {"date"}, {"datetime"},
      {"Literal"}, {"Any"}, {"Ref"}, or empty frozenset().
      type_mapper.py must return the correct frozenset for each ttype branch:
        - date ttype → frozenset({"date"})
        - datetime ttype → frozenset({"datetime"})
        - selection with static values → frozenset({"Literal"})
        - json/properties → frozenset({"Any"})
        - many2one → frozenset({"Ref"})
        - all others → frozenset()
      In codegen.py generate(), replace the 5 substring-scan lines (need_date/datetime/literal/any/ref)
      with:
        annotation, default, imports = pydantic_field_str(fs, self._in_set, _model_to_classname)
        need_date |= "date" in imports
        need_datetime |= "datetime" in imports
        need_literal |= "Literal" in imports
        need_any |= "Any" in imports
        need_ref |= "Ref" in imports
      The cross-import tracking for in-set m2o (fs.ttype == "many2one" ...check) is structural and
      stays unchanged — it reads fs.ttype directly, not the annotation string.
      Update all callers in codegen.py (generate method, field_lines.append uses only annotation and default).
      Test: test_type_mapper_returns_imports_set — call pydantic_field_str for date/datetime/selection/
        json/many2one/char fields, assert frozenset returned as third element contains the expected token.
      Test: test_generate_date_import_via_structural — schema with a date field; assert
        "from datetime import date" in generated source (regression guard that structural import works).
  </behavior>
  <action>
    Read codegen.py and type_mapper.py (already in context). Read test_codegen.py (already in context).

    Step 1 — Write failing tests in test_codegen.py (7 new test functions as described above).

    Step 2 — Fix type_mapper.py:
      Change signature: `def pydantic_field_str(...) -> tuple[str, str, frozenset[str]]:`
      Each return statement becomes a 3-tuple. Token names are single strings: "date", "datetime",
      "Literal", "Any", "Ref". Empty frozenset() for str/int/float/bool/unknown-fallback.
      Note: the "date" branch must NOT include "datetime" (they are separate). The "datetime" branch
      returns frozenset({"datetime"}). The unknown-fallback returns frozenset({"Any"}).

    Step 3 — Fix codegen.py:
      a. Add `import keyword` at the top (stdlib, unconditional).
      b. Define module-level constant _PYDANTIC_RESERVED_NAMES (frozenset as described).
      c. In _model_to_classname: after building `result`, add:
           if not result.isidentifier():
               raise ValueError(f"Model name {model!r} produces invalid Python identifier {result!r}")
           return result
      d. In the field-emission loop, add keyword and reserved-name guards after isidentifier() check.
      e. Replace annotation/default unpacking with 3-tuple unpacking for pydantic_field_str calls.
      f. Replace 5 substring-scan lines with set-membership checks on `imports`.
      g. Update both write_text calls to include newline="\n".

    Step 4 — Run tests. All 7 new tests + all existing codegen tests must pass.

    Commit message: `fix(introspection): codegen reserved-name/keyword/classname guards, LF writes, structural imports`
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-introspection/tests/test_codegen.py -x -q</automated>
  </verify>
  <done>
    All 7 new tests pass. All existing codegen tests pass. No mypy errors on
    packages/godoo-introspection/src with --strict. Generated source compiles (compile() test passes).
  </done>
</task>

<!-- ================================================================
     TASK D — godoo-introspection: CLI OdooError catch (#6)
     Files: cli.py, test_cli.py
     ================================================================ -->
<task type="auto" tdd="true">
  <name>Task D: Catch OdooError in CLI generate sync wrapper</name>
  <files>
    packages/godoo-introspection/src/godoo/introspection/cli.py
    packages/godoo-introspection/tests/test_cli.py
  </files>
  <behavior>
    Finding #6: the `asyncio.run(_generate_async(...))` block in generate() only catches ValueError.
    OdooAuthError, OdooNetworkError, and other OdooError subclasses propagate as raw tracebacks.

    Fix: extend the except clause to also catch OdooError. OdooError is already imported at that
    point in the function body (line ~66: `from godoo.client.errors import OdooError`). Change:
      except ValueError as exc:
          typer.echo(str(exc), err=True)
          raise typer.Exit(code=1) from exc
    to:
      except (ValueError, OdooError) as exc:
          typer.echo(str(exc), err=True)
          raise typer.Exit(code=1) from exc

    Emit just `str(exc)` — never the password or auth token (the OdooAuthError message does not
    include the password; this is safe per T-07-07).

    New tests in test_cli.py:
      test_generate_auth_error_exits_1 — monkeypatch asyncio.run to raise OdooAuthError("bad creds"),
        invoke CLI with full explicit credentials, assert exit_code == 1 and "bad creds" in output.
      test_generate_network_error_exits_1 — same but raise OdooNetworkError("connection refused"),
        assert exit_code == 1 and "connection refused" in output.
      test_generate_odoo_error_password_not_in_output — raise OdooAuthError("invalid login"),
        supply a known password string, assert password not in output (security regression guard).

    Import OdooAuthError and OdooNetworkError from godoo.client.errors in test_cli.py for these tests.
    Use monkeypatch on "asyncio.run" — patch it at the cli module's reference:
      monkeypatch.setattr("godoo.introspection.cli.asyncio.run", lambda *_: (_ for _ in ()).throw(exc_class("msg")))
    Simpler lambda: `lambda *a, **k: (_ for _ in ()).throw(exc_instance)` — or just define a raising
    function and pass it.
  </behavior>
  <action>
    Read cli.py and test_cli.py (already in context).

    Step 1 — Write 3 failing tests in test_cli.py.

    Monkeypatching pattern for raising from asyncio.run:
      def _raise(*_: object) -> None:
          raise OdooAuthError("bad creds")
      monkeypatch.setattr(asyncio, "run", _raise)

    Import asyncio at the top of test_cli.py. Also import OdooAuthError, OdooNetworkError from
    godoo.client.errors.

    Step 2 — Fix cli.py: change `except ValueError` to `except (ValueError, OdooError)`.
    OdooError is already imported inside the generate() function body; no additional import needed.

    Step 3 — Run tests. All 3 new tests + all existing CLI tests must pass.

    Commit message: `fix(introspection): catch OdooError in CLI generate wrapper`
  </action>
  <verify>
    <automated>uv run pytest packages/godoo-introspection/tests/test_cli.py -x -q</automated>
  </verify>
  <done>
    All 3 new tests pass. All existing CLI tests pass. No mypy errors.
  </done>
</task>

<!-- ================================================================
     FINAL QUALITY GATE (run after all 4 tasks)
     ================================================================ -->
<task type="auto">
  <name>Task E: Full quality gate</name>
  <files></files>
  <action>
    Run the full project quality gate to verify all changes are clean:
      uv run ruff check . && uv run ruff format . && uv run mypy packages/godoo-client/src packages/godoo-testcontainers/src packages/godoo-introspection/src
    Then run the full unit test suite:
      uv run pytest packages/ -m "not integration" -q

    Fix any ruff, mypy, or pytest failures. Do not proceed to the SUMMARY until all pass.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run mypy packages/godoo-client/src packages/godoo-testcontainers/src packages/godoo-introspection/src && uv run pytest packages/ -m "not integration" -q</automated>
  </verify>
  <done>
    ruff: no violations. mypy --strict: no errors on all three src/ trees.
    pytest: all unit tests pass, no failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Odoo wire → OdooBaseModel | Raw JSON data (False, tuples, strings) enters the validator |
| CLI args → asyncio.run | User-supplied credentials passed into async execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w2x-01 | Tampering | wire transform list coercion | mitigate | False→[] only for list-origin annotations; does not bypass bool D-02 guard |
| T-w2x-02 | Information Disclosure | CLI OdooError catch | mitigate | str(exc) for OdooError never includes the password (message is "invalid login" etc.); test_generate_odoo_error_password_not_in_output verifies this |
| T-w2x-03 | Tampering | codegen field-name guards | mitigate | Reserved names and keywords are skipped with warning, not silently renamed — no generated class has a poisoned field |
</threat_model>

<verification>
After all five tasks:
- `uv run pytest packages/ -m "not integration" -q` — all pass, no failures
- `uv run mypy packages/godoo-client/src packages/godoo-testcontainers/src packages/godoo-introspection/src` — no errors (--strict via pyproject.toml)
- `uv run ruff check . && uv run ruff format --check .` — no violations
- Manually verify: test_pydantic_transform.py has 3 new tests, test_typed_dispatch.py has 4 new tests,
  test_codegen.py has 7 new tests, test_cli.py has 3 new tests = 17 new tests total
</verification>

<success_criteria>
All 10 findings closed:
- #1 x2many False→[] ✓
- #2 search_read id injection ✓
- #3 m2o [id,False]→Ref ✓
- #4 reserved Pydantic names skipped ✓
- #5 Python keywords skipped ✓
- #6 OdooError caught in CLI ✓
- #7 ValueError→OdooValidationError at both dispatch sites ✓
- #8 invalid classname guard ✓
- #9 LF newlines in write_text ✓
- #10 structural import detection ✓
Quality gate green. No regressions in existing tests.
</success_criteria>

<output>
Create `.planning/quick/260601-w2x-fix-phase-06-07-code-review-findings-pyd/260601-w2x-SUMMARY.md` when done.
</output>
